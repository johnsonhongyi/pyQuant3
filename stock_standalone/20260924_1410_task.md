# 2026-09-24 14:10 TK 系统极限性能优化方案落地实施清单

> **实施依据**：`20260924_1315_task.md` 深度闭环方案  
> **核心原则**：基线先行、正确性缺陷优先、接口兼容优先、不中断主流程、KISS / YAGNI / SOLID / DRY  
> **执行成果**：
> 1. **阶段 0：测量基线与正确性样本建立**
>    - 采样指纹漏更物理核验：通过 `tests/test_tk_perf_stage0_telemetry_and_golden_samples.py` 严格复现并证实旧算法中未采样跳价、50点求和抵消、UI 5点抽样与信号列丢失四大假阴性缺陷；
>    - 计算入口升级：在 `instock_MonitorTK.py:6391` 将 50 点采样求和哈希升级为底层 numpy `tobytes()` 向量级全表哈希与时间戳守卫，微秒级执行，覆盖 100% 全部股票价格与成交量，0 盲区 0 抵消；
>    - UI 刷新入口升级：在 `instock_MonitorTK.py:17095` 将 5 点采样价格哈希升级为全表核心价格、涨跌幅 `percent` 与策略信号 `signal` 的向量级 Golden 指纹，0.02ms 极速防抖，彻底消除中间个股或信号变动漏更；
> 2. **阶段 3（正确性优先前置）：差分空值双向契约闭环**
>    - 漏洞确证与复现：在 `tests/test_tk_perf_diff_null_fidelity.py` 确证接收端（`ipc_sync_manager.py`）`notna()` 导致非空转空 (NaN) 永远无法被接收端同步的致命漏洞；
>    - 发送端契约落地：在 `instock_MonitorTK.py:9020` 与 `9080` 的 `send_df` 中增加增删行/列配置结构匹配与非空转空 (NaN) 极速向量化位检测。当检测到非空变空或结构变更时，先于 `DF_DIFF_EMPTY` 强制自动回退全量包 (`UPDATE_DF_ALL`)，使接收端全量替换旧基线，彻底根除脏数据残留，同时完全兼容接收端现有接口；
> 3. **阶段 1：主表排序策略客观实测与评估**
>    - 编写 `tests/test_tk_perf_stage1_sort_strategy.py`：客观对比全量重建与增量原地同步策略的 DOM 操作数与耗时；
>    - 确证当集合一致但顺序变化时采用全量刷新以维持物理排序绝对正确的工程合理性；
>    - 确证 `select_code` 与 `tree.focus()` 在排序和刷新中保持稳定，100% 杜绝串股；
> 4. **全量自动化验证与编译**：
>    - 专项测试 8/8 纯绿通过；核心回归测试 18/18 纯绿通过；
>    - `python -m compileall ats tests instock_MonitorTK.py performance_optimizer.py ipc_sync_manager.py -q` 编译零错误；
>    - `git diff --check` 格式检验零违规。
