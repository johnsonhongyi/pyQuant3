# 2026-09-24 14:10 TK 系统极限性能优化方案落地与审查深度闭环清单

> **实施依据**：`20260924_1315_task.md` 深度闭环方案（含第七节审查审计补充）
> **核心原则**：基线先行、正确性缺陷优先、接口兼容优先、不中断主流程、KISS / YAGNI / SOLID / DRY  
> **闭环执行成果**：
> 1. **P1 指纹漏更根除：消灭 XOR 归零抵消与未覆盖列**
>    - 缺陷复现确证：在 `tests/test_tk_perf_stage0_telemetry_and_golden_samples.py` 中严格复现“两列同值数据（如 `trade` 与 `price`）同步跳价时，无防护 XOR 异或合并哈希永远抵消归零”引发的致命静默漏更漏洞；
>    - 计算入口（`instock_MonitorTK.py:6391`）：全面升级为带列名的有序乘法哈希（Multiplicative Hash，`h * 31 + hash((col, bytes))`），彻底消灭多列异或抵消，并有序覆盖价格、成交量、`name`、`signal`、`percent` 及采集时间戳；
>    - UI 刷新入口（`instock_MonitorTK.py:17095`）：全面升级为有序乘法加权哈希，覆盖全表核心价格、涨跌幅、信号、名称及当前表格展示列，确保任何展示列更新即刻放行刷新；
> 2. **P1 日线全量回退发送门禁解耦（显示轨为空时日线不被拦截）**
>    - 致命逻辑阻断根除：审查确证旧代码在 `instock_MonitorTK.py:9265` 仅依据显示轨 `if msg_type == 'DF_DIFF_EMPTY': sent = True` 粗暴跳过后续全部物理发送，导致分时等非日线周期下显示轨为空时，日线轨因非空变空触发的 `UPDATE_DF_ALL` 全量回退包被整体静默抛弃；
>    - 发送门禁解耦（line 9265, 9335, 9365）：重构为双轨正交门禁 `if not has_display_update and not has_daily_update and not is_forced: sent = True`；只要日线轨有数据更新，必定进入分发循环，向 26670/26671/26675 订阅端正常发送日线数据包，彻底消除日线全量丢失隐患；
> 3. **P1 借读契约与发送基线闭环**
>    - 总线借读者防污染（line 9005）：严格履行阶段 2 借读契约，`send_df` 从总线取出快照后，附加 TWAP 等派生列前显式执行 `df_bus_all = df_bus_all.copy()` 隔离拷贝（Copy-on-Write），严禁原位修改总线内部快照；
>    - 异常与重连全量自愈：Pipe 断开或 Socket 发送失败时强制设置 `_cold_start = True`，重连后首包强制发送全量包重置基线；
> 4. **P2 阶段 0 与阶段 1 实测定位客观化**
>    - 阶段 1 实测定性校准：将测试结论明确标定为“保留现有全量刷新基线策略的实测依据”，客观量化了物理行索引位移的复杂性，不以模拟测试夸大替代真实 GUI 耗时；
> 5. **全量自动化验证与编译**：
>    - 阶段专项测试集 9/9 纯绿通过（覆盖未采样跳价、采样求和抵消、XOR 同值双列抵消、名称/信号变动、差分空值吞噬、双轨门禁解耦、排序保真度与防串股）；
>    - `python -m compileall ats tests instock_MonitorTK.py performance_optimizer.py ipc_sync_manager.py -q` 编译零错误；
>    - `git diff --check` 格式检验零违规。
