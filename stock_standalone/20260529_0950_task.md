# 2026-05-29 09:50 Task - Fully Fixed Look-Ahead Bias & Optimized Performance of Legacy Backtest

## 任务目标
- 对老的回测仿真脚本 `test_reentry_backtest_old.py` 内部高开销的 `df_curr = df_all.loc[:current_date]` 切片及 rolling 计算进行性能及未来偏向物理重构。
- 根治由于 `df_curr` 未定义导致的回测崩溃，并统一消除测试重定向产生的 BOM/UTF-16 编码冲突。

## 实施详情
- 修改 `scratch/test_reentry_backtest_old.py`：
  - 同样剥离循环内部切片与 rolling 重复计算，将全部指标指标化，并按全局 `row_idx` 执行 $O(1)$ 高性能局部获取。
  - 清洗并重构 `has_position` 及判定流中对 `df_curr` 的强引用，消除未来函数隐患。
  - 规范输入输出为标准的无 BOM UTF-8。

## 验证结果
- [x] 备份回测脚本消除未来函数偏向，运行流畅无报错。
