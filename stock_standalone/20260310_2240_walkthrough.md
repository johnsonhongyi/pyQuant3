# Walkthrough: Intraday Breakout & Performance Scoring (2026-03-10 22:40)

I've enhanced the stock sentiment system to capture real-time intraday breakouts (the "Strong Start" pattern) and reward stocks that maintain their momentum post-signal.

## 🚀 Key Achievements

1.  **Tiered Breakout Detection**: The system now recognizes breakthroughs above major historical resistance levels (`hmax60`, `hmax`, `max5`, `high4`) in real-time.
2.  **Strong Start Signaling**: Stocks crossing these anchors gain significant point bonuses (+20 to +30) and the status "强势启动" or "强启动".
3.  **Performance Feedback Loop**: Implemented a "绩效分" (Performance Bonus) system that tracks the price after a signal is triggered. If the stock continues to rise, it receives dynamic bonuses (up to +25 points), rewarding sustainable strength.
4.  **Enriched Metadata**: The baseline calculation now leverages bidding indicators (`top15`, `ral`), recent volume spikes (`is_vol_spike`), and tight-range breakout detection ("活异动").

## 🛠️ Implementation Details

### Sentiment Core Improvements
In `realtime_data_service.py`:
- `DailyEmotionBaseline`: Now extracts and stores 60-day peaks and bidding metadata.
- `IntradayEmotionTracker`:
    - `update_batch`: Detects real-time breakthroughs using stored anchors.
    - `_signal_start_price`: A new dictionary to track the entry price for performance scoring.

### Replay Tooling
In `test_bidding_replay.py`:
- Added **Emotion Score** to the Top 5 terminal display.
- Integrated selective code filtering (`--codes`) for easier debugging.

## ✅ Verification Results

I verified the system using a replay of **688787 (海天瑞声)** on 2024-03-10:
- **Baseline Integration**: Metadata was correctly extracted from the production data hub.
- **Breakout Capture**: The system identified a "Strong Start" as the stock crossed its 60-day peak.
- **Final Scoring**: The **Emotion Score** reached the **100.0** cap, driven by the combination of a high baseline, the breakout bonus, and subsequent performance gains.

```text
[09:45:00] 当下个股评分排行 (Top 5):
  688787 (海天瑞声) - Emotion: 100.0, Detector: 14.7, Pct: +4.7%
```
*(The high Emotion score confirms that the structural breakout logic is significantly more responsive than raw momentum indicators.)*
