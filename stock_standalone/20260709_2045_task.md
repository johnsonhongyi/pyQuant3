# 2026-07-09 20:45 任务清单：规划分析实盘资金流与1小时dff/放量数据结合的快速定位能力

## 当前任务
- [x] **设计1小时dff时间切片与放量数据结合的资金流定位方案**
    - [x] 分析1小时dff周期切片的聚集特征（优先出现、持续拉升、突然拉升）。
    - [x] 结合信号面板中的放量数据（如量比、分笔增量等），设计三阶段动态过滤与捕捉模型。
    - [x] 制定非侵入式（YAGNI/KISS）的系统级和UI级联动规划。
- [x] **编写/更新 `realtime_fundflow_positioning_plan.md` 规划分析方案**
- [x] **更新 `gemini.md` 变更日志**
- [x] **物理代码实现与自测验证 (Implementation & Verification)**
    - [x] **更新 `global.ini`**：将 `流速状态` 字段加入 `vol_up_details_col` 列表中以实现免重构动态列自动解析。
    - [x] **升级 `instock_MonitorTK.py`**：在 `_async_stats_aggregation` 定时聚合后台进程中，根据 1 小时时间切片（`0-15m 优先`、`15-45m 持续`、`45-60m 突发`）以及资金流偏离度 `dff` 与量比 `vr` 的多维度共振规则，实时计算并注入个股 `流速状态`。
    - [x] **美化 `signal_dashboard_panel.py`**：在 `VolumeDetailsDialog.update_data` 渲染方法中，为 `"流速状态"` 字段实现明亮青、黄、高亮红三色和加粗字体的 premium 渲染样式，使得资金流状态在列表中一目了然。
    - [x] **编写自测试脚本与物理管道验证**：编写 `scratch/test_fund_flow_positioning.py` 验证流速分类规则；编写 `scratch/test_ui_flow_status.py` 结合 PyQt6 环境进行完整的表格注入与渲染色彩自适应验证，全部断言 100% 成功通过。

