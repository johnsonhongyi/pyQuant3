# 2026-05-29 10:10 Task - Fully Resolved Active Branch Display Drift between Backtest and Visualizer

## 任务目标
- 根治回测最后一天输出的“活跃分支推荐”与主图实际通过最新指标计算的“最新活跃分支”不一致的物理漂移。
- 排除循环外不一致键名传入 `StrategyRouter.route` 导致的 Fallback 回退。

## 实施详情
- 修改 `scratch/test_reentry_backtest.py` 的仿真循环：
  - 弃用回测结束后外部手动组装 `sig.features` 重新调起 `StrategyRouter.route` 的粗暴做法。
  - 重构为在仿真主循环每日跑完 `decide()` 后，直接把当天最精确的 `intent.reason.routed_branch` 动态实时注入并更新回测结果字典 `_last_backtest_best_branch` 中。
  - 对齐持仓（`IN_TRADE`）和空仓观察（`FLAT`）双态下的每日路由结果。

## 验证结果
- [x] 力量钻石等标的最后一天路由被精确且零副作用地对齐，双端策略路由判定一致性达 100%。
