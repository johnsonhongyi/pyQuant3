# 20260611_2220_task

## 目标
实现独立自治交易决策系统（ATS v2）的完整实施方案。按照用户确认的重排优先级，无侵入式地分阶段构建 `ats/` 系统，并在不破坏 TK 现有接口的前提下实现全套功能。

## 任务清单
- [x] **初始化项目结构**：创建 `ats/` 目录并配置 `__init__.py` 等基础项。
- [x] **P0 阶段：Qt Dashboard 原型实现**：
    - [x] `ats/ui/styles.py`：构建统一风格 of QSS 配色系统。
    - [x] `ats/ui/main_window.py` 等 UI 原型：搭建主窗口、树形股票池、波段跟踪表、交易流水/持仓 Tab、以及市场热度（饼图/柱图）原型。
- [x] **P1 阶段：IPCBridge & SQLite 接入**：
    - [x] `ats/ipc_bridge.py`：实现只读数据库查询与基础配置载入。
    - [x] 结合 P0 原型：读取并展现真实的历史信号、交易流水与资金曲线。
- [x] **P2 阶段：UniverseManager 漏斗模型**：
    - [x] `ats/universe_manager.py`：实现雷达池、观察池、交易池的三层晋升与淘汰过滤。
- [x] **P3 阶段：SwingTracker 状态机**：
    - [x] `ats/swing_tracker.py`：实现 MA20 回踩企稳与出场状态机及推荐理由。
- [x] **P4 阶段：BacktestEngine 信号有效性分析**：
    - [x] `ats/backtest_engine.py`：历史信号胜率、盈亏比、最大回撤等回测指标统计。
- [x] **P5 阶段：TradeJournal 绩效统计**：
    - [x] `ats/trade_journal.py`：提取并格式化交易历史与策略胜率分类饼图。
- [x] **P6 阶段：SharedMemory & Queue 实时接入**：
    - [x] 对接盘中 `df_all` 行情共享内存与实时信号 `mp.Queue`。
- [x] **编译与回归测试验证**：
    - [x] 全量代码静态编译无死角校验。
    - [x] 运行全量集成测试保证零倒退。
