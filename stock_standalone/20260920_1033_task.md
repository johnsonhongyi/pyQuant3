# 2026-09-20 10:33 任务清单：修复系统 Gemini CLI 废弃并升级 Antigravity CLI

## 任务背景与目标
- **现象**：系统中的 `gemini` cli 在调用 Google API 时被废弃拦截，报错 `IneligibleTierError: This client is no longer supported for Gemini Code Assist for individuals. To continue using Gemini, please migrate to the Antigravity suite of products: https://antigravity.google`。
- **目标**：全面启用并配置 Google 官方全新的 Antigravity CLI (`agy`)，修正环境变量，构建 `agy` / `antigravity` / `gemini` 多别名兼容桥接层，无损导入旧版配置，并在桌面管理器中接入 CLI 状态探针。

## 待执行项
- [x] 1. 修正用户 User PATH 环境变量（加入 `C:\Users\Johnson\.gemini\bin` 与 `C:\Users\Johnson\AppData\Local\agy\bin`，清理失效路径）；
- [x] 2. 执行 `agy install` 配置环境与 shell 设置；
- [x] 3. 执行 `agy plugin import gemini` 从 `~/.gemini/` 导入旧版配置；
- [x] 4. 在全局命令目录构建 `agy.cmd` / `agy.ps1` 与 `antigravity.cmd` / `antigravity.ps1`；
- [x] 5. 将 `gemini.cmd` / `gemini.ps1` 改造为透明桥接转发至 `agy.exe`，彻底消灭废弃拦截；
- [x] 6. 在 `webTools/window_manager/antigravity_manager.py` 增设 CLI 探针与测试；
- [x] 7. 验证 `agy`、`antigravity`、`gemini` 运行与全量自动化测试（15/15 passed）；
- [x] 8. 同步更新 `gemini.md` 与总结报告。
