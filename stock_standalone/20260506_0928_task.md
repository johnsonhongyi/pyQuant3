## 修复 DataHub 导入异常与冗余调用 (Fixed DataHub Service Import Error & Redundant Call)
**Date:** 2026-05-06 09:28

### 目标 (Objectives)
修复在 `realtime_data_service.py` 的高频行情更新循环 `update_batch` 中, 由于尝试导入和调用已被移除的外部组件 `data_hub_service` 导致的 `ModuleNotFoundError` 异常及大量冗余的 ERROR 日志，确保行情更新逻辑精炼高效，消除 CPU 无谓消耗。

### 实施方案 (Implementation Plan)
- [x] **精细注释 DataHub 调用**：在 `realtime_data_service.py` 的 `update_batch` 方法中，将 2426-2430 行尝试导入和发布到 `data_hub_service` 的 try-except 块进行注释。由于 `DataHubService` 在系统前期重构中已被安全移除，此处属于遗留的未同步调用，直接注释掉不仅消除了连续报错，还减少了高频行情下的性能损耗。
- [x] **更新开发文档**：同步更新 `gemini.md` 中的【🔴 当前任务】及【变更日志】，保持项目开发跟踪信息的实时对齐。
