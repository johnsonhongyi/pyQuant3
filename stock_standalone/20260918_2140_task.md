# 2026-09-18 21:40 任务记录：恢复 MonitorTK 原有稳健数据推送频率与保护，剥离总线冗余唤醒

## 1. 现场指令与决策背景
- **操盘手核心指示**：
  > “不用还是恢复之前的同步频率即可.统一tk的全量主要提供基础数据分发,增量主要更新价格涨幅等.如果增量极度占用tk的性能,哪还是恢复原有的推送逻辑.只是添加的服务端流式常态订阅引擎即可”
  > “L6152-L6157 这些更新都不需要了”
- **根因回溯与架构共识**：
  - 过去在全量推流下，秒级广播导致 ATS 终端和多周期策略引擎发生严重卡顿与 GIL 挤占（历史上为此专门增加 `max(30.0, base_interval)` 与 180s/300s 保护）；
  - 增量推流目前尚未全面测试全市场 5500+ 股票对各客户端下游（直方图全表分箱、策略过滤 query.eval 树扫描、多周期通道扫频等）的冲击；
  - 遵循操盘手决策：完全恢复原有稳健的推送周期（交易期 30s，非交易期 180s），保留已实现的 `_stream_subscribers` 常态流订阅中心与单次强制请求处理机制。

## 2. 实施变更清单
1. **恢复推送频率与休眠保护 (`instock_MonitorTK.py:L8908-L8935`)**：
   - 恢复 `base_interval = float(getattr(cct, 'duration_sleep_time', ... 30.0))`；
   - 恢复交易时段 `dynamic_interval = max(30.0, base_interval)`，非交易时段 `max(180.0, base_interval * 3.0)`；
   - 恢复限流休眠 `time.sleep(1.0)` 小碎步平滑休眠，防止 CPU 100%。
2. **移除总线就绪时的冗余唤醒 (`instock_MonitorTK.py:L6152-L6157`)**：
   - 彻底删除 `_on_bus_data_ready` 中触发 `self._send_df_wake_event.set()` 的多余调用，防止总线每来一次数据就打破休眠节拍，彻底保持原有节奏。
3. **保留服务端流式常态订阅引擎 (`_stream_subscribers`)**：
   - 保留对 26670 (ATS), 26671 (多周期), 26675 (新股次新) 及动态端口的注册与常态化分发，不引入破坏性变更。

## 3. 验证与回归
- `tests/test_ipo_command_room_persistence.py`：4/4 PASSED (100%)；
- `tests/test_ipo_fleet_trading_arbitration.py`：8/8 PASSED (100%)。
