# 2026-09-20 22:28 任务清单：Antigravity 独立客户端与 Antigravity IDE 双目标同步解耦与运行时感知

## 任务背景与目标
- **现状**：系统中有两套客户端：
  1. Antigravity 桌面独立客户端（`C:\Users\Johnson\AppData\Local\Programs\antigravity`，数据库 `%APPDATA%\Antigravity\User\globalStorage\state.vscdb`）；
  2. Antigravity IDE 编辑器（`D:\JohnsonProgram\AntigravityIDE`，数据库 `%APPDATA%\Antigravity IDE\User\globalStorage\state.vscdb`）。
- **痛点**：原管理器界面默认只显示“同步至 IDE”，操盘手在只用 Antigravity 客户端时无法明确同步给客户端，容易搞混且不知道当前使用的是哪一个。
- **目标**：
  1. 架构级支持显式双目标同步（`sync_to_app` 与 `sync_to_ide`），彻底解耦；
  2. 运行时进程与数据库感知：实时检测当前运行的是 IDE 还是 Antigravity，并在 UI 顶部状态灯实时展示；
  3. 支持自由切换同步目标（Antigravity 客户端 / IDE / 两者同时），卡片切换账户与顶部同步按钮逻辑清晰、绝不搞混；
  4. 完善单元测试与端到端验证。

## 待执行项
- [x] 1. 在 `antigravity_manager.py` 中重构双数据库路径常量（`APP_DB_PATH` / `IDE_DB_PATH`，保留兼容别名）；
- [x] 2. 新增运行时探针 `get_runtime_app_status()`，实时探测进程和数据库活跃度；
- [x] 3. 新增定向同步核心函数 `sync_to_target(target="app"|"ide"|"both")` 与多模式 `switch_account`；
- [x] 4. 在 UI 顶部栏新增实时运行状态指示徽章（展示当前运行的是 IDE 还是 Antigravity）；
- [x] 5. UI 顶部栏增设专属 `[🚀 同步至 Antigravity]` 与 `[💻 同步至 IDE]`，并支持全局同步目标切换；
- [x] 6. 编写全量单元测试覆盖定向同步与状态探测；
- [x] 7. 运行自动化测试与功能验证（17/17 PASSED）；
- [x] 8. 同步更新 `gemini.md` 与总结报告。

## 验收与落地成果
1. **双 Tab 彻底解耦独立入口 (`🚀 Antigravity 客户端` 与 `💻 Antigravity IDE`)**：
   - 彻底废除“双向同步/双向对齐”带来的相互覆盖与串号 bug；
   - 对话框主体采用标准独立的 `QTabWidget` 双入口：
     - **Tab 1【🚀 Antigravity 客户端】**：
       - 专属状态徽章：实时展示独立客户端运行态（`🟢 客户端运行中`），明确呈现当前在用账户为 `Johnson Zou <j***i@gmail.com>`；
       - 卡片专属呈现：Johnson Zou 卡片高亮翠绿徽章【🟢 客户端在用】，其余卡片为【⚪ 备用账户】；
       - 专属切换按钮：卡片底部为清晰明了的 `[🚀 切换给 Antigravity]`，点击纯粹且仅写入独立客户端数据库 `APP_DB_PATH`，绝不干扰 IDE；
     - **Tab 2【💻 Antigravity IDE】**：
       - 专属状态徽章：实时展示 IDE 编辑器运行态（`🟢 IDE运行中` 或 `⚪ 离线`），明确呈现当前在用账户为 `弘逸 <h***8@gmail.com>`；
       - 卡片专属呈现：弘逸 卡片高亮深蓝紫徽章【🟢 IDE在用】，其余卡片为【⚪ 备用账户】；
       - 专属切换按钮：卡片底部为清晰明了的 `[💻 切换给 IDE]`，点击纯粹且仅写入 IDE 数据库 `IDE_DB_PATH`，绝不干扰客户端；
2. **测试残留垃圾账号彻底清除与防污染**：
   - 彻底清理本地账户目录残留的 `two@example.com.json` 垃圾文件；
   - 单元测试与真实账户目录实现彻底沙箱隔离。
3. **全量自动化与回归验证 100% 绿灯**：
   - `pytest stock_standalone/tests/test_antigravity_manager.py` 17/17 PASSED 全部通过；
   - `pytest stock_standalone/tests/test_tdx_wildcard_matching.py` 5/5 PASSED 全部通过。
