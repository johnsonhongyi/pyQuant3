# 2026-05-29 09:30 Task - Aligned Backtest Style, Implemented Marquee Status Bar & Non-Modal Window Reuse

## 任务目标
- 将回测报告窗口重构为非模态独立窗口（可通过 parent 挂钩），使用户可以边看报告边与主 K 线图交互。
- 窗口支持单例复用，支持置顶勾选、四种护眼配色方案动态热切换，且所有状态跨会话自动保存。
- 为主窗口状态栏引入自定义的 `MarqueeLabel`（跑马灯滚动标签），彻底解决超长日志强行撑大主界面的几何拉伸缺陷。

## 实施详情
- 修改 `trade_visualizer_qt6.py`：
  - 将 `ScrollableMsgBox` 的打开方式由模态 `exec()` 重构为非模态 `show()` 并加入 parent owned 挂钩，全局维护单例 `self._backtest_report_dlg`。
  - 在对话框左下角新增 `QCheckBox("置顶")` 和 `QComboBox("配色")`。配色提供柔和银灰、科技淡绿、护眼浅黄、高对比白四种配置，选择时动态刷新 HTML `<pre>` 样式。配置自动持久化于 `visualizer_layout.json`。
  - 编写 `MarqueeLabel` 自定义类，在文本超出视口时驱动定时器循环滚动，并在 setText/clear 时同步触发 updateGeometry。

## 验证结果
- [x] 回测报告热切换配色、置顶及实例复用流畅，布局无高分屏拉伸漂移。
- [x] 状态栏跑马灯平滑滚动，主视口几何框架永久稳定。
