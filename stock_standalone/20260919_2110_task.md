# 2026-09-19 21:10 任务计划：新股检测中心与集中交易指挥室长期通道企稳底部结构次级买点实盘交易闭环

## 任务背景与核心目标
- **用户明确要求**： ats的sbc实现了底层数据功能,也在实现TDXRealtimeFetcher 策略信号,研究如何实现 长期通道后企稳,有个底部结构后次级买点信号的能力,规划一个实施方案如何实现信号检测,开平仓策略,对齐TDXRealtimeFetcher 中如何优化,但这阶段不实施、这是新股检测工具及交易决策中心的功能实现、写出完善计划,不要实施。
- **目标**：彻底解决由异动弱提示迈向今日实盘买入决策的深层能力断层，制定一套集结构状态机、强信号分级（S0~S5）、标准化 TradePlan、全生命周期买卖点绑定、L0~L2三层极限性能漏斗与 SBC 图元映射的完整工程级规划。

---

## 阶段规划清单 (纯方案规划，不实施代码修改)

- [x] **方案设计与架构制定 (P0)**：
  - [x] **现状缺陷破案**：诊断异动提示与实盘买入决策之间的核心断层（缺乏时序前后文、买卖点未强绑定、多周期无差别拉取吞吐瓶颈、缺乏风控闸门）；
  - [x] **量化模型定义**：确立长期通道企稳、平底箱体扎底（Base Low）、首次长阳试盘（First Breakout）、缩量回踩抬升（Higher Low）、次级放量突破确认（Secondary Buy）的数学公式与阈值；
  - [x] **架构解耦与职责边界**：
    - TDXRealtimeFetcher：仅负责取数保活、多周期 K 线 TTL 缓存与轻量指标，移除业务买卖点；
    - channel_secondary_buy_strategy.py：独立负责通道计算、平底识别与结构状态机流转；
    - IPOVWAPDetectorEngine：集成 L0/L1/L2 过滤漏斗与 S0~S5 信号分级；
    - IPOTradingCenter：构建不可变 TradePlan、情绪阶段闸门、赛马冒泡排位与全仓轮动接力；
    - ProactiveExitEngine：回溯开仓 TradePlan 实施 Higher-Low 结构止损、通道中轴保本推移与时间止损；
    - IPOSubnewDetectorDialog / IPOCommandRoomDialog / SBCIntradayChartDialog：前端自适应表格呈现、领头羊卡片与 SBC 四点结构线映射。

- [ ] **Stage 1: 策略模型与状态机独立模块 (待审批后实施)**：
  - [ ] 新增 ts/strategy/channel_secondary_buy_strategy.py；
  - [ ] 实现 evaluate_channel_secondary_buy() 与 ChannelSecondaryBuyStateMachine；
  - [ ] 编写独立仿真单元测试（	ests/test_channel_secondary_buy_strategy.py）。

- [ ] **Stage 2: TDXRealtimeFetcher 取数与多周期二级缓存优化 (待审批后实施)**：
  - [ ] 增加 60F（30s TTL）与日K（180s TTL）内存级二级缓存；
  - [ ] 解耦买卖点策略逻辑，保持 Fetcher 接口纯粹。

- [ ] **Stage 3: 新股检测引擎接入与 L0/L1/L2 漏斗调度 (待审批后实施)**：
  - [ ] 在 IPOVWAPDetectorEngine 实现 L0 盘口快筛 -> L1 结构推演 -> L2 深度确认；
  - [ ] 扩展 VWAPDetectorSignal 包含结构状态与价格网络。

- [ ] **Stage 4: 交易决策中心 TradePlan 闭环与持仓保护联动 (待审批后实施)**：
  - [ ] 在 IPOTradingCenter 实现不可变 IPOTradePlan 生成与 S0~S5 闸门；
  - [ ] 联动 ProactiveExitEngine 注入 higher_low_stop 与阶段目标。

- [ ] **Stage 5: 前端 UI 呈现与 SBC 分时图图元联动映射 (待审批后实施)**：
  - [ ] 在检测中心表格展示结构状态与建议；
  - [ ] 在指挥室展示 TradePlan 战术卡；
  - [ ] 在 SBC 分时图绘制 Base/Break/HL/Buy 四大结构坐标线。
