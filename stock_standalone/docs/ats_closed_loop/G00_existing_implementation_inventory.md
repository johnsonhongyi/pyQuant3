# G00 已有实现盘点

日期：2026-09-24  
方式：源码/测试盘点与缺口修复；未读取或写入生产 DB/日志。G01–G05 及 G09 基线专项 68 项通过；加入 G06 新专项后的合并命令 69 项通过。生产 DB/日志未读取或写入。

| 任务 | 实施前核对 | 判定与已确认缺口 |
| --- | --- | --- |
| G00 | 有用户提供的 9/23 运行态审核结论；本轮补采 9/24 当前进程路径、PID/启动时间和相关 EXE SHA256；ATS 版本资源为空且安装目录无 fingerprint/manifest sidecar | **部分/阻断线上验收**：接口契约和当前运行二进制快照已记录；历史 51/19 逐代码差异、原始源数据哈希、当前 EXE 到源码提交的构建对应和 0 BUY 逐层漏斗仍缺证据。 |
| G01 | `t1_position_facts.py`、`snapshot_v2.py` 及专项测试已存在 | **源码与离线验收完成，线上待验**：修复多于快照的流水误判、未知批次解锁和 v1/v2 校验问题；G02 已把迁移校验接入 PAPER 状态恢复。G01 专项 10 项通过；未用线上快照验证。 |
| G02 | PAPER adapter 有 JSON 快照恢复、流水推导和持仓告警 | **部分，离线缺口已补，线上差异已采样未解释**：只读样本为快照 51、订单规则推导 19；32 个快照独有代码在现存 716 条流水中无记录，19 个重合代码数量一致；报告 `ledger_status=LEGACY_MISMATCH`。流水起始时间晚于快照历史，且订单行没有成交状态；根因、现金/T+1事实和零差异仍未验。 |
| G03 | 主路径有唯一 `event_key` 和 SQLite UPSERT，现有测试覆盖基本幂等/历史重复 | **核心实现已存在，跨表关联未实现但不能判为重复缺陷**：已修 direct UPSERT 竞态、历史键迁移、`evaluated` 单向和来源维度去重，并阻止无效价格进入 live history。复核 `stock_live_strategy.py` 的同一异步回调：`signal_message` 是候选/计数队列，`live_signal_history` 是带价格的轨迹表；该路径传给队列的 `extra` 不含价格，因此只由显式 logger 写一次历史，未发现同表重复。历史表没有 event_id，跨表追溯仍是证据缺口；因其职责和去重周期不同，本轮不擅自合并。 |
| G04 | CandidateCache、盘前 seed 隔离、连续帧确认均已存在 | **源码与离线验收完成，线上待验**：补齐 UTC+08 市场时钟、注入时钟、纯时间字符串的确定性日期、午休/跨日/超时和重复乱序边界；专项 24 项通过。各线上数据源时间戳编码尚未核验。 |
| G05 | LedgerUpdateService、SignalLedger 与独立 Lifecycle 状态机均已存在 | **源码与离线验收完成，线上/UI 待验**：原有服务门禁、快照投影与双 VWAP 失效保护保留；将 Lifecycle 接入真实账本和 TDX 晋级路径，状态转移写入 `to_dict()` 快照并携带原因、时间、snapshot_id、revision。新增跨日破位终态及生命周期序列化回归。线上状态呈现与多视图一致性由 G10 验收；旧快照读入仍按跨日候选重建，不恢复旧生命周期对象。 |
| G06 | 有 T+1 当日卖出阻断、同标的 EXIT 压 BUY 和一般退出逻辑 | **部分实施，历史专项/线上未验**：延期退出专项及底台止损/高潮直接指令的规则身份回归通过；已补直接 SELL 指令的 `exit_rule_id`、规则层级并验证审计 envelope。9/22 历史 118/2 失败夹具未找到并同版本复现，全量策略回归和线上 PAPER 多日闭环仍未完成。 |
| G07 | ATS 到 TK PAPER 通路存在；TK Gateway/adapter 有 request_id/order 幂等 | **Gateway 重试源码与定向离线验收通过，线上待验**：ATS 传 `directive_id` 并回写双腿审计；拒绝响应按同指纹允许重评，不同 payload 仍冲突；指挥室现继承 TK 内核/流水对账状态及 PAPER 恢复阻断原因，缺少内核报告时 fail-closed；fake-gateway/对账 5 项与 Gateway 契约 7 项通过，仅用隔离 fake service，未连接 PAPER 账户。 |
| G08 | 检测器已有日内量比归一化、分日 VWAP 和跨日趋势特征 | **负样本过滤代码已补，合成专项 4 项通过**：覆盖跨日弱势负样本降为 WATCH、WATCH 经 ATS 指令生成路径无 BUY、正向样本可继续 PRE_ORDER、CLIMAX_EXIT 优先；冻结历史样本、正式 S4/S5 回放、阈值召回/误杀和线上效果待验。 |
| G09 | 潮汐分类器已有多状态阈值、风险降级与转移原因 | **最短状态保持代码已补，专项 11 项通过**：覆盖原状态 9:59 保持、10:00 转移、T2 风险即时生效及既有因果/跨日刷新用例；更广阈值抖动回放和线上状态指标待验。 |
| G10 | UniverseManager 和两处 UI 已显示走弱/失效徽章 | **动作入口专项 4 项通过，桌面验收待做**：失效/过期 BUY 在 UI 右键执行前被拦截且未调用交易中心；假菜单测试验证禁用状态和阻断 tooltip；UniverseManager 与排名表共用生命周期分类，覆盖首见涨幅为正转负；指挥室账户 tooltip 新增内核/流水对账状态及 PAPER 恢复阻断原因；真实桌面按钮及审计追溯仍待 GUI 验收。 |
| G11 | 影子脚本和健康报告骨架已存在 | **ATS 决策影子采集及显式隔离 TK PAPER 注入已补，runner 专项 7 项通过**：无代码 dry-run 降级；默认路径仍断开；隔离路径必须位于报告目录内，TK adapter、KernelService 和 Gateway 均注入独立实例；流水异常写 DEGRADED 报告。真实行情逐笔链路和 2–3 日报告未验。 |
| G12 | 监控器原有 UI 任务累计耗时 | **阶段性 P50/P95/P99 采样源码已补，运行态基线待采**：区分 MarketStateBus 快照、DataFrame 对比/复制、序列化、Pipe/各 Socket 发送；仅 IPC 样本可独立输出，最近秩分位数逻辑经隔离探针验证。上游行情供应商调用和线上性能修复仍待运行态归因。 |
| G13 | 原无统一日报工具，首版仅按平铺 JSONL 解析 | **只读 ATS/TK 源结构及报告维度已补，schema 专项 10 项通过**：可读 ATS `signal_iteration_log` JSON 快照和 TK 嵌套 JSONL；坏的非对象记录会降级报告；新增同代码/动作/60 秒窗口的诊断性跨源/策略重叠基线；按跨日路径统计字段覆盖、新鲜度、漏斗、拒绝、MFE/MAE、滑点和 T+1 延迟损失；真实审计源字段分布仍需只读验收。 |
| G14 | 原回放器已有 7 项检查，联合发布校验器与指纹工具已存在 | **12 项回放生产逻辑已补；本轮发布门定向 32 项通过、14 文件集成回归 114 项通过；发布证据仍不齐**：新增失效买入、RR≥2.5、T+1 召回、重复订单、T+1 当日买入批次卖出检查；影子报告须匹配运行 commit、fingerprint、EXE SHA256，标记隔离 PAPER，逐指令对齐结果/请求审计，坏行阻断；G13 日报须含可计算重叠基线；账户还必须明确 `paper_execution_ready=true`，布尔值不得冒充数量、价格或时间戳。严格 ALIGNED、无差异、多日影子等仍需真实运行证据，未构建/部署，当前只能 MONITOR_ONLY。 |
| G15 | 无同一构建的集成结论 | **已形成只读验收结论：未通过**。G02 的 51/19 差异已定位到 32 个无流水快照代码，但根因和零差异证据不足；G07 专项、完整影子闭环和发布证据未齐，禁止标记整体完成。 |

## 结论与实施边界

- 当前工作树分组复验：G02 28 passed；G03/G04/G05（含部分回归）26 passed；G06 9 passed；G07 指令/对账 5 passed、Gateway 契约 7 passed；G08/G09 15 passed；G10 4 passed；G11 7 passed；G13 10 passed；G14 发布门相关 32 passed。测试集存在交叉，不对各组数量求和；结果均为隔离离线用例，不能代替线上/PAPER 证据。
- 这不是 16 项从零开发。后续按表中缺口增量修改，已有功能只补验收和边界保护，不重写已满足项。
- 生产数据只读；无原始数据证据时不得宣称账户差异为零或线上构建匹配。PAPER 以外执行保持禁止。
- G01 变更只涉及 `trading_kernel/t1_position_facts.py`、`trading_kernel/snapshot_v2.py` 和 `trading_kernel/tests/test_t1_position_facts_v2.py`；当前 `paper_adapter` 恢复路径已调用 snapshot v2 迁移与校验，G02 离线专项覆盖该集成入口。G01 源码/离线实现已完成，线上快照恢复尚未验，因此线上验收仍待完成。
- G00 原始基线证据未齐，所有发布验收继续阻断；线上运行观察、连续 2–3 日影子报告和实际 EXE 指纹不能用离线源码/单测替代。
- G03/G05 修复仅覆盖表中指出的入口与状态边界；G04 离线专项通过不代表线上候选行为和 PAPER 闭环验收通过。
- G02 回归使用隔离临时快照；PAPER 不匹配会阻断执行，但未读取现网持仓文件，因此不能声称现网对账结果为零。
