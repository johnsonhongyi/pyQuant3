# G08 量能与跨日 VWAP 结构

状态：负样本过滤代码已补；专项回放及线上验证未完成。实施前检查确认检测器已有日内量比归一化、分日 VWAP 与 5 日趋势特征，但缺少“昨收/尾盘处于 VWAP 下、窄幅蓄势，今高开冲高回落”的显式过滤。

新增跨日审计字段包含前日收盘/VWAP/振幅、今开缺口、日内高点回撤、过滤版本和命中标志，并显式记录 `cross_day_state=CLEAR/FAILED_GAP_FADE`。TradePlan 复制该状态，进入指令审计信封和 TK 请求特征。当前严格组合条件为：前日收盘及末 3 根收盘均低于 VWAP、前日振幅不超过 5%、今开高于昨收至少 2%、从日高回撤至少 1.5%，且现价低于开盘与 VWAP。命中后保留 CLIMAX_EXIT 优先，并返回 WATCH，阻止其被后续正向结构合成为 BUY。

专项测试 `pytest tests/test_g08_cross_day_vwap_filter.py -q`：5 passed。覆盖负样本特征审计并降为 WATCH、检测器输出经 ATS 指令生成路径不产生 BUY、正向对照仍可进入 PRE_ORDER、同一负样本下 CLIMAX_EXIT 优先级保持。该合成夹具不等同于冻结历史样本回放；正式 S4/S5 回放、阈值召回/误杀、正样本保持能力仍须验收，缺失/错序行情输入的稳健性和线上效果未验。


2026-09-24 追加上下文闭环：信号计算结果被复制到 TradePlan；交易中心在指令批次发布前以同批 sentiment 和同代码 signal 填充 strategy_tag、	ide_state、cross_day_state。缺少跨日计算标记的 BUY 保留缺值，避免用未知值伪装完整。检测器与指令上下文专项包含在本轮 G06/G07/G08/G13 39 项回归中；没有真实 PAPER 轨迹或历史冻结样本。
