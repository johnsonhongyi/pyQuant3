# G09 潮汐状态滞回

状态：源码缺口已补；专项边界回放和线上验证未完成。实施前检查确认分类器已有多阈值状态、转移原因和急跌状态，但没有最短状态保持，短时指标波动可反复切态。

新增默认 10 分钟最短状态保持（可由配置 `min_state_hold_minutes` 覆盖）；同一会话按连续实时状态计时。T0 样本不足及 T1/T2/T4/T11 风险状态立即生效，不受保持时间阻挡；每次延迟切换会记录 `minimum_hold:<state>` 原因。原分类阈值未改，计时滞回降低边界频繁翻态。

专项回归 `pytest tests/test_g09_state_hysteresis.py tests/test_subnew_tide_state_machine.py -q`：11 passed。新增回归覆盖 9 分 59 秒保持、满 10 分钟切换及 T2 风险即时生效；完整阈值边界抖动、线上状态指标仍未验。
