# 【明日开盘实战部署计划书】新股次新股检测中心与集中交易中心 (2026-09-22 实战检验版)

## 一、 部署目标与核心宗旨

> **核心目标**：确保明天（2026-09-22）开盘 9:30~15:00 期间，操盘手在集中交易指挥室与新股检测中心中：
> 1. **消灭早盘假阳性脉冲噪声**：早盘 9:30~10:00 绝不被虚假放量和脉冲冲高误导开仓；
> 2. **只出极高确定性买卖点**：只有形态质量 $\ge 2.5:1$ 盈亏比、经回踩抬高确认的次级买点（`S4/S5`）才进入待执行列表，全天买入候选收敛至 1~3 只；
> 3. **底线风控绝对坚挺**：同代码退出决议绝对压制买入，T+1 物理锁合规，零重复下单，零幽灵持仓。

---

## 二、 明日实战时间表与关键节点

```
[08:45 - 09:15] 盘前自检阶段：账本对账、TK构建指纹校验、环境与行情源确认
[09:15 - 09:25] 集合竞价阶段：IPO竞价抢筹感知 (IPO_BID_SURGE)，底仓今日可卖股数同步
[09:30 - 10:00] 早盘首轮博弈：时段投影归一化生效，阻断早盘假突破；静默观察 S0~S3，仅放行 S4
[10:00 - 11:30] 盘中主升与回踩：长期通道底部次级买点 (CHANNEL_SECONDARY_BUY) 黄金确认窗口
[13:00 - 14:30] 午后防守与轮动：ProactiveExitEngine 动态防守位生效，监控破位出局
[14:30 - 15:00] 尾盘决策结算：清点未执行过期买点，生成日终对账快照与复盘归因
```

---

## 三、 今晚投产上线部署最小闭环切片 (P1 精准防噪声)

在明日开盘前，仅实施以下两项对“消灭早盘噪声”有决定性作用的高性价比切片：

### 切片 1：早盘成交额分时投影归一化 (P1-01 降噪门禁)
- **痛点**：早盘 9:35~9:45 集合竞价集中撮合导致短时放量假象，算法容易误判为“全天巨量突破”。
- **实施**：
  - 引入基于交易时段标准累计成交比例（如 9:35 约占全天 8%~12%）的时段投影换算；
  - 早盘前 15 分钟放量倍数需经衰减因子折减校正，杜绝 1 分钟脉冲诱多触发 S4。

### 切片 2：指挥室待执行候选门限硬核卡紧 (S4/S5 强门槛)
- **痛点**：过去盘中有股票稍微异动就推送指令，导致操盘手眼花缭乱。
- **实施**：
  - `S0`（背景噪声）、`S1`（脉冲试盘）、`S2`（蓄势观察）、`S3`（地量就绪）坚决留在新股监控池内部静默观察，**严禁推送到待执行指令区**；
  - 只有达到 **`signal_level == "S4"` 且 `quality_grade in ("S", "SS", "A")` 且 盈亏比 $\ge 2.5:1$** 的标的，才生成不可变 `TradePlan` 并进入指挥室的 **【⏳ 直接买卖点】**。

---

## 四、 实战运行监控与操作纪律

1. **执行模式锁定**：
   - 默认保持 `PAPER`（模拟执行）与 `CONFIRM`（人工确认）模式；
   - 严禁擅自开启真实券商 `LIVE_AUTO`。
2. **直接买卖点人机协同**：
   - 当指挥室出现 `[S4 探路买入(30%)]` 时，操盘手点击行可弹窗核查：
     - 触发价（`trigger_price`）
     - 防守止损位（`higher_low_stop`）
     - 预期中轨目标（`target_1`）
   - 若符合主观盘感与大盘情绪，手工点击“一键执行”或观察 PAPER 自动撮合。
3. **熔断与红线**：
   - 遇大盘情绪处于 `T1_CLIMAX_DISTRIBUTION`（高潮派发）或 `T4_PANIC_ACCEL`（恐慌加速）时，系统自动拒绝一切买入并显示拒绝码，操盘手坚决不逆市开仓。

---

## 五、 部署执行步骤清单 (Runbook)

1. [ ] **Step 1（今晚）**：代码改动合入与 `git commit`（阶段三已完成部分与统一入口固化）；
2. [ ] **Step 2（今晚）**：轻量接入早盘时段投影归一化与 S4 强过滤门槛；
3. [ ] **Step 3（今晚）**：全套自动化测试回归（目标 56+46+43 passed，compileall 零报错）；
4. [ ] **Step 4（明早 09:00）**：启动主程序，检查 `BUILD_FINGERPRINT.json` 与启动对账快照 `latest.json`；
5. [ ] **Step 5（明早 09:30）**：实盘盯盘，观察直接买卖点收敛表现并记录首日日志。
