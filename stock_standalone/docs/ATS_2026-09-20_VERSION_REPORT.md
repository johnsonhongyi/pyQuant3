# ATS 2026-09-20 版本报告

## 版本结论

- 当前能力：真实60分钟数据、次级买点、主动退出、T+1硬锁、交易复盘、事件情绪、T0-T11潮汐状态、实时仓位闸门。
- 当前发布性质：模拟决策与回放验证版本，不是无人监管实盘版本。
- 物理开关：`AUTO_MERGE=OFF`、`AUTO_REAL_BUY=OFF`、`AUTO_REAL_SELL=OFF`。
- 用户原有 `gemini.md` 修改未纳入下列提交。

## 今日提交明细

### `0f305f5f` 主动退出与T+1防守接线

- 将 `ProactiveExitEngine` 接入 `IPOTradingCenter`。
- 建仓时登记 TradePlan，支持分批减仓与全部退出。
- 增加T+1生成及执行双重硬锁、冷启动账本恢复。
- 未连接真实券商交易网关。

### `158fb366` 真实60分钟K线注入检测链

- 由共享TDX底层提供真实60分钟数据。
- 移除“日线冒充60分钟线”的失真回退。
- IPO扫描工作线程批量预取60分钟数据。

### `a9740b44` 退出幂等与T+1绕过加固

- 防止相同退出规则在连续刷新中重复生成卖单。
- T+1绕过仅允许明确的灾难性硬止损规则。
- 普通减仓、换马和非灾难退出不能伪造绕过标志。

### `ddb4ec5d` Antigravity临时认证失败重试

- Agent Orchestrator 识别明确的临时认证异常并自动重试一次。
- 代理网络 `Eligibility EOF` 不误判为认证错误。
- Antigravity CLI与编排器预检通过。

### `6844864a` TradePlan执行复盘指标

- 计算计划风险、计划收益、计划盈亏比与实际收益。
- 支持内存数据和冷启动字典快照。
- 平仓结果可反向追踪到初始 TradePlan。

### `879d42fd` RamDisk离线历史回放修复

- 修复非交易时段或TDX断线时错误返回空数据的问题。
- 从 `G:/tdx_global_cache_pool.pkl.z` 重建静态分钟历史。
- 合并增量缓存时执行时间去重与排序。

### `59e81bc6` 真实TDX新股回放样本

- 固化688826与601091真实分钟缓存聚合的60分钟样本。
- 688826真实回放达到 `PULLBACK_STABLE`，不伪造 `SECONDARY_BUY`。
- 601091只有首日4根60分钟线时明确返回数据不足。

### `6c4cf25b` 全仓轮动测试对齐T+1规则

- 修复旧测试要求当日买入后立即卖出的错误预期。
- 验证当日轮动被拒绝，下一交易日才允许执行。
- 消除全局单例状态造成的测试顺序污染。

### `e72fd20c` 可回放市场上下文风险闸门

- 增加稳定序列化的市场上下文快照与快照ID。
- 修复“天量高潮”判断分支永远不可达的问题。
- 数据源异常自动降级，狂热或天量环境压缩开仓预算。
- Git标签：`ats-signal-core-v0.6.0`。

### `a6948f69` 次新股时点事件情绪引擎

- 按 `as_of` 严格截断事件，禁止回测读取未来信息。
- 支持发行节奏、机构需求、首日赚钱效应、行业催化、破发、监管与供给压力。
- 事件可以提前提升观察优先级，但不能单独生成买单。

### `076de17d` 次新股T0-T11潮汐状态机

- 建立退潮、恐慌、冰点、背离、修复、回流、涨潮和高潮状态流转。
- 恐慌加速禁止接飞刀；冰点和冰点背离仅开放5%/15%试错上限。
- 前缀回放与完整回放一致，验证无未来数据泄漏。
- 判断失败后自动反向修订状态和仓位动作。

### `27a91c01` 实时潮汐总控与仓位闸门

- 从盘中信号聚合上涨广度、日内VWAP承接、涨跌中位数、成交额、头尾弹性和样本完整度。
- 同一交易日高频刷新按盘中覆盖处理，不伪装成多个交易日。
- 将潮汐状态、置信度、仓位上限、轮转动作和纠错原因写入市场快照及交易中心总览。
- 最终开仓预算取原有情绪预算与潮汐绝对上限中的较小值。
- 验收：60项联合回归通过，`compileall` 和 `git diff --check` 通过。

## 数据回放观察

基于RamDisk中31只八月以来上市新股的完整截面：

| 日期 | 潮汐状态 | 核心依据 |
|---|---|---|
| 09-08 | T3 退潮扩散 | 上涨16.7%，站上日VWAP 29.2% |
| 09-09 | T4 恐慌加速 | 中位跌幅-2.57%，上涨19.2% |
| 09-10 | T5 冰点 | 日VWAP承接11.1%，跌势开始钝化 |
| 09-11 | T6 冰点背离 | 成交额升至167亿，头部20%平均上涨8.19% |
| 09-14 | T9 涨潮扩散 | 上涨与站上日VWAP均为82.1% |
| 09-15 | T2 退潮初期 | 上涨17.9%，日VWAP承接降至0% |
| 09-16 | T8 回流确认 | 上涨63.3%，日VWAP承接80.0% |

09-17截面只有2只标的，不满足全市场判断要求，必须降级为数据不足。

## Agent协作事实

- GPT/Codex负责总体架构、任务拆解、代码审查、测试闸门和版本决策。
- Agent Hub任务001/002已执行过Antigravity Worker与Codex审查闭环。
- 本报告后半段的事件情绪、潮汐状态机和实时总控接线由GPT/Codex直接实现并验证。
- 这些新节点尚需通过Antigravity独立回测任务进行第二视角复核，不能把单Agent测试等同于双Agent交叉验收。

## 后续任务

1. 由GPT生成Antigravity独立审计任务，只允许读取本次潮汐相关文件和真实回放样本。
2. 增加盘中分钟级历史回放，验证状态抖动、纠错延迟和仓位换手成本。
3. 输出按潮汐状态分组的次日收益、最大回撤、胜率、盈亏比和误报率。
4. 仅在模拟盘验证通过后接入模拟下单，不直接开启实盘。
5. 真实下单仍需单独的人工授权、券商网关审计和熔断演练。
