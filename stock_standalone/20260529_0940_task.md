# 2026-05-29 09:40 Task - Implemented Extreme Backtest Engine Performance Optimization

## 任务目标
- 消除回测主循环中的高开销行为，解决对每一天数据做 DataFrame 物理切片以及在其上重复进行 rolling 均值/标准差计算的性能瓶颈。
- 保证优化后的计算结果与老版本 baseline 完全一致（100% 财务与决策等价）。

## 实施详情
- 修改 `scratch/test_reentry_backtest.py` 的仿真循环：
  - 将所有日线均线与 Bollinger 带等 rolling 指标计算全部移到主循环启动前进行全量预计算。
  - 在循环中，使用行索引物理指针 `row_idx` 以 $O(1)$ 常数时间直接提取所需指标特征，将主循环算法复杂度由 $O(N \times M)$ 极限优化至 $O(N)$。
  - 引入路由加载静态全局标记 `_is_router_loaded` 缓存，消除对 `global.ini` 路由配置的重复物理磁盘 IO 读取。

## 验证结果
- [x] 多只代表个股（蓝色光标、通富微电、百合花、力量钻石等）字符级整体报告进行二进制完全对账，结果 100% 毫无差别契合。
- [x] 回测性能大幅飙升，执行流畅度极其显著。
