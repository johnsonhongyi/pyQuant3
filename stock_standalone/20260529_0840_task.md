# 2026-05-29 08:40 Task - Align Context Menu Backtest with Alt+X Behavior

## 任务目标
- 在 `instock_MonitorTK.py` 中的右键菜单触发 Re-entry 历史回测时，不要显示 "正在计算" 的 Toplevel 进度提示小窗口。
- 让右键菜单触发的回测与 `Alt+X` 快捷键触发的回测行为完全一致（无小窗口，直接静默后台多线程计算，计算完成后弹出独立回测报告对话框）。
- 对齐股票代码 Emoji 物理清洗逻辑，保障代码参数干净。

## 实施详情
- 修改 `instock_MonitorTK.py` 的 `_on_run_reentry_backtest_menu` 方法：
  - 彻底去除创建、更新、销毁 `progress_win = tk.Toplevel(self)` 的冗余 UI 逻辑。
  - 在入口处加入股票代码的清洗逻辑，移除 `'🔴', '🟢', '📊', '⚠️'` 等 Emoji 字符。
  - 异步子线程中，正常计算仅获取精简回测总结报告，若遇异常则直接将异常栈信息回传在回测报告弹窗中展示。
- 更新 `gemini.md` 顶部的开发日志，对齐全平台进度。

## 验证结果
- [x] 代码已就绪，已完美去除进度小窗口，右键触发逻辑直观干净。
- [x] 回测核心单元测试验证无任何副作用。
