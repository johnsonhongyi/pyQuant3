# 任务清单 - 2026-09-19 00:25: 新股检测工具轮询间隔极简下拉框与文字宽度自适应

## 背景与目标
1. 解决新股检测工具中自动轮询间隔硬编码问题，增加极简下拉框（5秒/10秒/15秒/30秒/60秒），默认 15 秒；
2. 修复冷启动股票池自愈门禁与方法兼容性；
3. 优化 UI 视觉：按钮不再重复显示 "(30s)"，保持清爽 "⏳ 自动轮询: 开"，下拉框自适应文字宽度；
4. 支持 `poll_interval_sec` 自动持久化与冷启动恢复。

## 核心完成任务
- [x] 1. `ats/ui/ipo_subnew_detector_dialog.py`:
  - 顶栏加入 `combo_interval`，采用 `setSizeAdjustPolicy(AdjustToContents)` 自适应文字宽度；
  - 按钮文案剔除重复的秒数后缀，开启时统一保持为 `⏳ 自动轮询: 开`，关闭时为 `⏳ 自动轮询: 关`；
  - `poll_interval_sec` 全链路持久化至 `config/ipo_detector_layout.json`；
  - 修复 `re` 模块缺失与 `get_combined_new_stocks` 兼容调用。
  - **全屏自适应列宽消灭黑边**：重写 `resizeEvent`、`changeEvent` 与 `showEvent`，实现 `adjust_columns_to_viewport()` 动态计算视口物理宽度，将多余空间平滑分配给【操作建议 / 为什么 (预下单逻辑)】长文本列，窗口全屏大尺寸下右侧 0 黑边，窗口缩小时提供 220px 保护并自然启用横向滚动条；
  - **严格遵守持久化准则**：移除 `<= 5` 强塞 35 只默认股票逻辑，冷启动 100% 忠实还原用户持久化池子；清除 `_on_scan_finished`（后台自动扫描完成）、`_auto_sync_bottom_ipo_stocks`（底层增量合并）、`_load_persisted_state`（加载读操作）中的非手动写盘调用，仅在操盘手手动操作（加/删代码、重置、调间隔、切开关、关闭窗口）时才持久化。
- [x] 2. `ats/ui/intraday_strategy_dialog.py`:
  - **SBC 默认尺寸升级**：由原先过于局促的 680x420（甚至 522x436）升级为图2 舒适尺寸 `800x560`，顶栏 18 个功能按钮与走势图元彻底舒展；
  - **关闭最后一个 SBC 的持久化尺寸生效**：重构 `_restore_sbc_geometry`，确保打开任何 SBC 窗口严格优先使用持久化的 `sbc_window_size`，彻底杜绝被个股历史陈旧的平铺小尺寸 (`code_geo`) 篡改覆盖；
  - **隔离一键平铺临时缩放尺寸**：平铺重排 (`rearrange_all_sbc_windows`) 多窗口网格布局不再污染 `_global_sbc_size` 与 `sbc_window_size`，标准尺寸只由单窗与关闭持久化确定。
- [x] 3. `tests/test_ipo_persistence_and_auto_sync.py`:
  - 扩展至 11 项专属单元测试（涵盖下拉框持久化、全屏铺满视口消灭黑边、纯手动持久化与少数标的忠实还原、SBC持久化尺寸严格使用与默认 800x560 验证），全套 11 项测试 100% 全部通过。
