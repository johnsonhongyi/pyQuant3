# 2026-09-18 09:55 任务成果汇报

## 任务目标
解决线上 TDX API 早盘集合竞价无数据、未上市股票导致批次查询整包失效、主站假死误切换与超短检测工具涨跌幅与现价未更新问题。

## 核心修改与成果
1. **`ats/new_stock_fetcher.py`**：
   - 建立未上市股票物理隔离门禁：严格将股票区分为“已上市交易标的”与“待上市新股”；
   - 待上市新股 100% 物理拦截，严禁传入通达信实时盘口 API，彻底拔除导致通达信整包丢弃的 9 只未上市毒丸代码。

2. **`ats/tdx_realtime_fetcher.py`**：
   - 引入深度递归二分拆包队列（Recursive Binary Partitioning），遇异常批次自动递归二分，精准识别并单只隔离毒丸代码，保证其余 39 只正常标的毫秒级返回；
   - 主站假死切换前增加基准存活标的（000001/600519）探针校验，探针存活绝不误判假死，根治了通达信频繁断线重测速震荡。

3. **`ats/strategy/ipo_vwap_detector_engine.py`**：
   - 补齐 `sig.change_pct` 涨跌幅精确计算（对接上一交易日收盘价昨收）；
   - 在 09:15~09:30 集合竞价期（此时通达信尚未生成当日 1 分钟 Bar）自动融合 `fetch_stock_snapshot` 最新竞价盘口，现价与涨跌幅实时动态跳动，不再死等分钟线。

4. **自动化测试**：
   - 新增 `tests/test_tdx_quotes_robustness_and_change_pct.py`（3/3 PASSED）；
   - 回归 `tests/test_ipo_subnew_detector.py` 与 `tests/test_tdx_realtime_fetcher.py`（25/25 PASSED）。
