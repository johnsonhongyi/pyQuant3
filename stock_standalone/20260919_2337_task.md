# 任务清单：长期通道底部结构次级买点 P1 界面呈现与交易指挥室卡片升级 (2026-09-19 23:37)

## 任务背景
在 P0 阶段完成独立策略模块 `ats/strategy/channel_secondary_buy_strategy.py`、交易中心接入、主动退出引擎联动与 TDX 分级 TTL 缓存的基础上，推进 P1 阶段：将长期通道企稳与次级买点信号完整接入检测引擎、在检测中心表格高亮展示形态阶段/防守价，并在集中交易指挥室卡片呈现不可变 TradePlan 网格。

## 核心任务切片 (P1)
- [x] 1. **检测引擎与数据接入 (`ats/strategy/ipo_vwap_detector_engine.py`)**
  - 在 `VWAPDetectorSignal` 增加 `channel_stage`, `channel_stage_cn`, `higher_low_stop`, `base_low_invalid`, `trade_plan`, `quality_grade`, `strategy_tag` 字段。
  - 在 `analyze_stock` 流程中接入 `evaluate_channel_secondary_buy`，多周期共振计算通道企稳与次级买点，生成不可变 TradePlan 并融入赛马打分与决策。
- [x] 2. **新股与通道检测工具界面呈现 (`ats/ui/ipo_subnew_detector_dialog.py`)**
  - 表格【VWAP结构形态】列直观展示【形态阶段】彩色徽章（`👑 次级买点` / `⏳ 缩量回踩` / `🚀 首次试盘` / `📦 平底箱体` / `📉 通道寻底`）。
  - 表格【信号评级】统一展示 `S4 | SS`、`S5 | S` 等标准生命周期与质量分级。
  - 表格【极窄止损位】智能对齐 `higher_low_stop` 抬高底防守位。
  - 操作建议列展现不可变 TradePlan 买入网格与止损提示。
- [x] 3. **集中交易指挥室卡片与指令升级 (`ats/ui/ipo_command_room_dialog.py`)**
  - `ROLE_CN_MAP` 增加 `SECONDARY_BUY: "👑 次级买点"`。
  - 待执行指令表格支持 `BUY_SCOUT` / `BUY_CONFIRM` 彩色标识，并在决议依据中清晰展示 TradePlan 买入网格、防守价与盈亏比。
- [x] 4. **自动化测试与全量回归**
  - 编写 `tests/test_channel_secondary_buy_ui_and_engine.py`。
  - 运行全部关联测试，确保 100% 绿灯 (54/54 全部 PASSED)。
