# 2026-09-24 13:15 TK 系统性能优化方案（深度闭环修订版·纯规划·不实施）

> **文档性质**：系统性性能与资源架构优化规划（只规划方案，不修改/不实施既有代码）  
> **面向系统**：`instock_MonitorTK.py` (主监控系统)、`data_utils.py` (数据采集与计算管线)、`performance_optimizer.py` (表格更新器)、`market_state_bus.py` (行情总线)、`ipc_sync_manager.py` (跨进程同步接收端)  
> **核心约束**：以原始行情输入和期望输出建立 Golden Samples；严格保全发送端与接收端的差分空值语义；不作未经验证的绝对收益承诺；将高侵入性改造设为候选，以实测数据驱动准入。

---

## 一、审核复核与事实判断纠偏（闭环校准）

结合多次代码审查与专业审核结论，对现有系统行为的事实判断与优化策略进行严格纠偏：

### 1. 采样指纹漏更新与静默丢帧风险（纳入阶段 0 基线核查）
- **事实定位**：
  - 计算入口指纹 ([`instock_MonitorTK.py:6391-6410`](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/instock_MonitorTK.py#L6391-L6410))：在 5000 行数据中仅等间距抽取最多 50 行的价格与成交量计算哈希；
  - 主表刷新指纹 ([`instock_MonitorTK.py:17055-17075`](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/instock_MonitorTK.py#L17055-L17075))：仅抽取 5 行的一个数值列（如 `trade` 或 `percent`）计算哈希。
- **潜在危害**：
  在 `query`、`force` 等条件未触发且代码/列配置未变时，未采样个股的价格变化或仅名称、板块、均线、信号列变化，可能被相应入口的采样指纹判为“无变化”并跳过本次处理或刷新。计算入口还采用样本数值求和，采样点内正负变化相互抵消也可能漏判；下游是否还有强制刷新或其他通路，需在阶段 0 逐帧核对。
- **纠偏规范**：
  阶段 0 将“采样指纹漏更新/静默丢帧率”列为首要测量项。Golden Samples 保存原始行情、历史状态和用户操作输入，以绕过待测指纹的独立参考计算生成预期结果；仅靠单帧原始行情不足以推导有历史依赖的信号。逐层比对实际计算与展示路径，区分暂时跳过、后续补齐和持续遗漏。

### 2. 阶段 1 客观定位：主表排序策略比较与逐行移动评估（摒弃“误判”定性）
- **事实定位**：
  [`instock_MonitorTK.py:17098`](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/instock_MonitorTK.py#L17098) 中 `set(current_codes) == set(self._last_df_codes) and current_codes != self._last_df_codes` 触发 `force_full = True`，是现有系统在代码集合相同但顺序变化时**为了维持物理排序绝对正确所采取的有意策略**，而非简单的无意识误判。
- **纠偏规范**：
  不使用“排序误判”或“主表 0 侵入”这类表述。阶段 1 的任务是**对比几种排序策略在不同变动比例下的综合成本与交互行为**：
  - 策略 A（基线）：现有全量清空重建（评估大数量级下的 Tcl DOM 重建开销）；
  - 策略 B：增量更新 + 移动优化（需设计算法识别可保持相对行序的子集，单纯对比每行原/目标索引不等于最小移动序列）；
  - 策略 C：保留现有物理行的分批重排；“仅重绘视口”需重构物理行映射，归入阶段 4 单独评估；
  必须在异步批次全部提交并由事件循环完成布局更新后，实测行序、焦点与选中稳定性，以及用户可感知的最长阻塞；LIS 可作为最小移动数的候选算法，但还须验证 `tree.move` 执行顺序、插删行和中途交互。

### 3. 差分协议接收端契约与空值语义完整闭环
- **事实定位**：
  [`ipc_sync_manager.py:340-366`](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/ipc_sync_manager.py#L340-L366) 的接收端合并逻辑：
  ```python
  if isinstance(df_diff.columns, pd.MultiIndex):
      # 仅提取 val_type == 'self' 的列
      df_diff = pd.DataFrame(new_cols, index=df_diff.index)
  col_data = df_diff.loc[common_idx, col]
  valid_mask = col_data.notna()
  self.current_df.loc[valid_indices, col] = df_diff.loc[valid_indices, col]
  ```
  现行接收端仅提取 `self` 层级，且**强制使用 `notna()` 过滤**。
- **关键矛盾**：
  如果发送端发生“某个指标/信号由非空值变为 NaN（如信号失效重置）”，由于接收端用 `notna()` 跳过了空值，这个 NaN **永远无法写入接收端**！导致接收端残留脏数据。
- **纠偏规范**：
  差分协议优化必须包含**发送端与接收端的双向契约闭环**：
  - **路径 1（保持现有协议边界）**：对显示轨和日线轨各自检测非空转空及无法安全编码的结构变化，先于 `DF_DIFF_EMPTY` 判定回退 `UPDATE_DF_ALL`；每个接收端须以该轨全量包替换旧基线；
  - **路径 2（协议版本化升级）**：协同升级所有接收端（`ipc_sync_manager.py`、`ats/ui/main_window.py`、`trade_visualizer_qt6.py` 等），定义显式空值编码协议（如带 mask 的增量包）；
  - 无论采用何种路径，均须用全量快照与差分累加结果逐帧对照验证一致性；单纯退回旧 `DataFrame.compare()` 不能修复当前接收端的空值跳过。

### 4. 数据所有权与不可变边界的可执行规则（取代“封印”口号）
- **事实定位**：
  Pandas DataFrame 引用与版本号无法阻止运行态修改。现有系统在总线发布后，仍存在后台线程与主线程的混合写入点（如 TWAP 注入、情绪评分赋值、自选股标记）。
- **可执行所有权契约**：
  必须制定明确的《数据生命周期表》，界定“谁拥有、谁借读、何时复制、旧版本何时释放”：
  1. **独占写阶段（Single-Writer Stage）**：按各轨列出生产者、实际写入点与先后依赖；现有 `send_df` 在总线读取后仍调用 `attach_multiday_twap_to_df`，须先调整或保留隔离拷贝，不能宣称目前所有衍生列均已在发布前算齐；
  2. **只读发布点（Frozen Publish Point）**：仅在确认后续无写者的轨道评估发布引用。开发期可组合底层数组只读标志、关键写 API 包装和发布前后内容校验；`df[col].values.flags.writeable = False` 对新增列、Pandas 内部复制及扩展类型不是完备防写机制，不能保证抛异常；
  3. **借读契约（Borrowing Contract）**：`send_df` 广播线程与 UI 渲染线程仅作为**借读者（Borrowers）**，只读读取快照；若某分析模块需要派生临时列，必须显式调用切片拷贝（Copy-on-Write），严禁原位修改；
  4. **版本生命周期**：记录借读者持有、缓存、延迟任务与外部引用；由代码审计和内存采样验证旧版本实际释放时点，不以 Python 引用计数作为已消除跨代滞留的保证。

### 5. 撤销未经验证的性能与恢复承诺（回归科学工程态度）
- **撤销“10ms 极速自愈”**：Windows 环境下进程创建（`mp.Process.start()`）通常需要数十毫秒至数百毫秒，且从磁盘反序列化复杂状态机需严格耗时，不应虚构 10ms 承诺；
- **撤销“主进程完全释放 GIL”**：主进程仍然承担 UI 事件分发、过滤、排序及差分协议计算，优化目标是消除主进程中超额的纯 CPU 信号计算，而非脱离实际的“100% 释放”；
  - **完善自愈机制设计**：快照覆盖 `_state_dfs`、`_cached_data` 等跨帧状态，并绑定已完成计算的输入版本；临时文件和目标文件须位于同一卷，写完校验后再替换，并验证 Windows 文件占用下的失败行为。补帧重放的前提是存在连续、可重放的输入帧日志；坏快照或缺帧时须定义冷启动/全量重算与信号降级边界，不能承诺无损恢复。

### 6. 四个新增热点的实事求是核对
1. **主线程 `update_idletasks()` ([line 2573](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/instock_MonitorTK.py#L2573))**：
   每 5 个任务调用一次。20ms 调度预算仅在任务之间检查，单个慢任务仍可超时。先分别统计该调用与任务本身的耗时，并回归布局更新，不妄称“屏幕撕裂”，仅在实测证明其构成主要阻塞且移除后布局稳定时才调整；
2. **概念板块统计 ([line 17826](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/instock_MonitorTK.py#L17826))**：
   其统计仅基于 `head(50)` 且摘要结果常驻主窗口状态栏，不能以详情窗口隐藏为由跳过摘要更新。更值得测量的是重建概念标签的成本；应按完整输入版本与展示状态分别控制摘要和详情更新；
3. **自选股管理器 ([line 661](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/performance_optimizer.py#L661))**：
   所列调用均在各批处理循环外，主要开销是 `get_favorite_stocks()` 每次持锁生成新集合。列为低优先级测量项，评估在单次更新批次内复用只读集合，同时确保自选股变更时及时失效；
4. **字符串格式化 ([line 588](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/performance_optimizer.py#L588))**：
   `Series.map('{:.2f}'.format)` 仍逐元素调用 Python 格式化函数，并非 C 向量化。全量准备路径已按列预格式化，增量路径才逐行处理。应分开计时两条路径，重点验证 NaN、异常值及显示精度一致性。

---

## 二、准入驱动的演进路线图（非线性推进·门禁严明）

采用“基线先行、正确性缺陷优先、性能候选按净收益准入”的决策顺序。阶段 2 的所有权审计是必做的设计核查，去拷贝是有条件的改造；阶段 3 的空值正确性路径即使 `compare` 不慢也应单独处理。

```
  【阶段 0：测量基线与正确性样本建立】 (必须执行：提取真实性能，排查采样指纹丢帧)
                 │
                 ▼
  【阶段 1：主表排序策略比较与局部评估】 (完成对比，按实测决定是否改动)
                 │
                 ▼
  【阶段 2：总线所有权与生命周期审计】 (必须核查；去拷贝须另过安全与收益门禁)
                 │
                 ├─────────────────────────────────────────────────┐
                 │ 空值正确性必核查；差分提速有条件启动              │ 仅当主表仍为主线程瓶颈时启动
                 ▼                                                 ▼
  【阶段 3：差分正确性闭环；提速候选】             【阶段 4（候选）：视口虚拟化架构灰度】
                 │                                                 │
                 └────────────────────────┘

  【阶段 5（独立候选）：信号检测状态化迁移】 (信号计算确为瓶颈且净收益为正)
  【实验性探索项：共享内存、GC 阈值、数值降型】 (与主线无依赖关系)
```

---

## 三、各阶段技术方案详案

---

### 阶段 0：测量基线与正确性样本建立（Telemetry & Golden Samples）

#### 1. 目标
复用既有记录点，端到端测量各环节耗时分布；重点排查现有采样指纹的丢帧现象，建立独立于现有输出的正确性真值样本（Golden Samples）。

#### 2. 具体规划
1. **端到端计时与现有记录点提取**：
   - 提取 [`instock_MonitorTK.py:17120`](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/instock_MonitorTK.py#L17120) 的 `self.perf_monitor` 真实历史统计；
   - 提取 [`instock_MonitorTK.py:9027`](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/instock_MonitorTK.py#L9027) 的 `timed_ctx("viz_df_compare")` 与 `daily_df_compare` 真实耗时分布；
   - 提取主线程调度队列的 `_cycle_audit` 与 `_record_latency_sample` 记录；
   - **补齐端到端耗时测量**：定义“从收到行情包到最后一个异步渲染批次（`_chunked_insert`）完全完成”的真实端到端完成时间，避免把仅仅提交任务到调度队列误当成刷新完成。
2. **采样指纹丢帧现象排查（正确性门禁）**：
   - 对计算入口与主表指纹分别构造未采样个股跳价、采样点求和抵消、两列相同字节哈希经 XOR 抵消、仅名称/板块/均线/信号变化、排序及强制刷新案例；为每帧标记 `query`、`force`、版本、预期是否变化和实际跳过原因。
   - 指纹须覆盖实际影响计算或展示的代码、列和值；数值、对象和扩展类型应按内容稳定编码。有序乘法组合能消除已知 XOR 归零特例，但有限位哈希仍不能保证零碰撞；不能只检查 `current_cols` 前 10 列。无法安全编码时采用版本标记或全量处理。
   - 统计逐入口假阴性率、持续遗漏时间和恢复路径；先判定实际受影响的场景，再决定修复优先级。
3. **五大典型场景基线采样与 Golden Samples 固化**：
   - 场景 A（冷启动）、场景 B（稳态更新）、场景 C（排序洗牌）、场景 D（条件过滤）、场景 E（客户端推送）；
   - 记录 P50/P95/P99、主线程最大连续占用时长、进程 CPU 与 RSS；
   - **Golden Samples 固化**：保存原始行情及必要的历史状态、筛选/排序条件和预期 `df_all`、`ui_df`、Treeview 文本/tags、客户端全量重建结果；参考计算绕过待测指纹，并核对浮点/空值和显示格式。独立测试助手不能替代真实入口回放。
   - 性能数字须附机器、行情规模、dtype、重复次数和 P50/P95/P99；将哈希、空值扫描、序列化、渲染直到最后异步批次完成分别计时。

---

### 阶段 1：主表排序策略比较与局部评估（Table Order & Movement Hardening）

#### 1. 目标
在保持现有 `Treeview` 物理行结构的范围内量化排序策略；测量无显著收益时保留基线实现。

#### 2. 具体规划
1. **三种排序策略实测对比**：
   - 审查 [`instock_MonitorTK.py:17098`](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/instock_MonitorTK.py#L17098) 的全量触发判定与 [`performance_optimizer.py:724-727`](file:///d:/MacTools/WorkFile/WorkSpace/pyQuant3/stock_standalone/performance_optimizer.py#L724-L727) 的逐行移动逻辑；
   - 在场景 C（排序洗牌）下分别测量：
     - **策略 1（现有全量清空重建）**：记录 5000 行清空重插的真实端到端耗时与主线程挂起时间；
      - **策略 2（LIS 候选移动序列）**：对代码唯一且新旧集合相同的排列，用 LIS 计算理论最少移动数，并验证具体 `tree.move` 执行顺序与实际耗时；增删行另行处理；
      - **策略 3（现有物理行分批重排）**：比较分批调度与总完成时间，避免以降低单次阻塞掩盖更长的整体刷新；视口重绘留待阶段 4；
2. **业务一致性验收门禁**：
   - 无论采用何种策略，异步批次全部提交并完成后，主表最终行序必须 100% 严格等于 DataFrame 排序结果；
   - 选中行 `self.select_code` 与焦点 `tree.focus()` 在数据刷新后位置保持准确，不发生串股；
    - 快捷键提取的代码和名称 100% 正确；单元格文字与颜色 tags 完全与基准样本一致。
   - 测试刷新中途输入、连续两帧排序和过滤切换；用最大单回调耗时与最终完成时间共同择优，乱序度高时允许回退现有全量路径。
   - 使用真实 Tk Treeview 与生产 `TreeviewIncrementalUpdater`、`restore_selection` 路径测 5000 行及焦点/滚动；MockTreeview 的操作计数只能验证模拟算法，不能证明 Tcl 耗时或真实交互正确性。

---

### 阶段 2：总线所有权界定与只读快照防拷贝（Ownership & Immutability Boundaries）

#### 1. 目标
理顺数据流水线的实际写入者与数据寿命，在保持快照一致性的前提下，逐项评估并安全消除多余拷贝。

#### 2. 具体规划
1. **建立《数据生命周期与所有权契约》**：
   - **独占写阶段**：列出各轨的生产者、发布前后写入点、别名关系及消费者；现有 `send_df` 在读取总线后注入 TWAP，应先处理其依赖或为该消费者保留独立副本。
   - **只读发布点**：对确已结束写入且不再通过别名共享可写底层块的轨道，才评估引用发布；同时定义旧版本保留和释放条件。
   - **调试期违规写检测**：数组只读标志只覆盖部分原位修改，需结合写 API 检查、快照内容校验与并发回放；不得以 `flags.writeable=False` 保证 `df['new_col']=...` 会抛异常。
2. **逐项评估可消除的 `.copy()`**：
   - 针对 `MarketStateBus.publish` 中的 4 处拷贝路径，结合各轨实际非空状态与后续借读者生命周期，逐项评估安全移除的可行性；保留必要的生命周期隔离。
3. **业务一致性验收门禁**：
    - 在高频并发读取、生产者持续更新与旧版本滞留回放中，快照数据与版本号始终对应，且无已知写入路径污染旧版本；
   - 内存分配带宽与峰值有确切的实测下降指标。

---

### 阶段 3：差分正确性闭环与候选提速（Full-Column Fidelity Diffing）

> **准入门槛**：阶段 0 发现差分重建错误时，先处理正确性，不受性能占比限制；仅在 `DataFrame.compare` 的绝对耗时和占比共同构成瓶颈、替代方案有净收益时启动提速。

#### 1. 目标
定义显示轨与日线轨各自的发送、接收、基线版本和空值契约；在正确性通过后再评估向量化差分。

#### 2. 具体规划
1. **空值语义与接收端契约闭环**：
    - 审查 `ipc_sync_manager.py`、`ats/ipc_bridge.py`、`ats/ui/main_window.py`、`trade_visualizer_qt6.py` 等实际接收端的 MultiIndex、`self`、`notna()`、新增行/列及重连基线行为；
   - 规划双端契约：
      - 若维持旧协议：发送端逐轨检测非空转空、增删行、列/索引及无法安全表达的 dtype 变化，先于空差分判定发送对应轨的 `UPDATE_DF_ALL`；接收端以全量包替换旧基线；
      - 若升级新协议：差分包显式携带“单元格发生变化”的掩码及版本，所有接收链在兼容旧客户端的条件下同步升级；
   - 两轨的发送门禁必须独立：显示轨 `DF_DIFF_EMPTY` 时，日线轨仍可能需要 `UPDATE_DF_ALL`；核对 Pipe、Socket、订阅端的实际发送条件、冷却时间、发送间隔及各自版本。
   - 更新上一帧缓存与已发送版本的时点须区分；某订阅端在节流窗口错过全量回退帧时，后续差分不能基于该订阅端未收到的帧。发送失败或被节流后须持有待发全量状态，直到真实发送并确认或重新建立基线。
2. **分类型向量化比对**：
   - 对数值列（Float/Int）、字符串/分类列（Object/Str）分块比对；正确处理 `NaN`、`None`、`pd.NA` 的相等性；
    - 任何增删行或列/索引结构不一致均先回退全量包，除非所有接收端已验证相应增量语义；不以“变化幅度大”作为唯一条件。
3. **业务一致性验收门禁**：
    - 分别对两条轨逐帧回放全量与差分消息，真实接收端合并结果逐列等于该帧全量真值；覆盖显示轨不变但日线轨变空、空值双向转换、增删行、发送失败、乱序和重连。
   - 严格保留现有动态订阅、静态端口探测、临时动态端口与首次连接必须发全量包的物理规则。

---

### 阶段 4（候选）：视口虚拟化架构与交互状态机（Virtual Scrolling Viewport）

> **准入门槛**：阶段 1 后主表仍是经分项测量确认的主要交互瓶颈，且物理行数量而非单个慢回调或标签重建是主因；门槛数值由阶段 0 基线和交互目标确定。

#### 1. 目标
验证基于逻辑主键映射的虚拟化视口是否降低延迟，同时保留已列出的操盘交互语义。

#### 2. 具体规划
1. **基于 `stock_code` 的逻辑主键映射层**：
   - 废除以 Treeview 物理 iid 识别股票的逻辑，引入逻辑选中管理器 `SelectionManager`；
   - 视口滚动或刷新时，根据 `self.select_code` 自动反查并高亮对应的虚拟物理行；若当前选中股票已滚出屏幕，明确定义焦点和快捷键行为（锁定所选股票代码，操作不串股）；
2. **虚拟滚动条与交互事件接管**：
   - 根据底层真实总行数计算虚拟滚动条滑块大小与位置；
   - 拦截键盘 `Up`/`Down`、`PageUp`/`PageDown`、鼠标滚轮，映射为数据行偏移，平滑驱动视口切片。
3. **业务一致性验收门禁**：
    - 虚拟化作为独立配置开关灰度测试；出现焦点错股、快捷键目标错误等正确性问题立即回退；滚轮抖动先按可复现条件及延迟门槛判定；
   - 键盘连续翻页、快速切码、表头双击排序、单元格双击穿透与右键菜单等交互与现有主表完全等价。

---

### 阶段 5（候选）：`detect_signals` 状态化迁移与子进程解耦（Stateful Signal Migration）

> **准入门槛**：阶段 0 证明 `detect_signals` 的主进程 CPU/GIL 占用与主事件延迟存在可复现关联；再用原型测得进程启动、序列化、IPC、快照与恢复后的净收益为正。该候选独立于阶段 3/4。

#### 1. 目标
将重度 CPU 运算移入子进程，同时保障信号状态跨周期连续性与子进程重启自愈。

#### 2. 具体规划
1. **依赖拓扑梳理**：
   - 理清情绪评分与信号检测的真实依赖顺序，确保输入数据流时序正确；
2. **跨进程状态快照与原子自愈**：
   - 完整盘点 `SignalManager` 的 `_state_dfs`、`_cached_data` 及跨帧状态；
    - 快照绑定最后完成计算的输入版本与输出版本；临时文件和目标文件在同一卷，写完并校验后替换，测试 Windows 打开句柄/占用失败；仅在持久化成功后公布可恢复版本。
    - 补帧重放须有连续输入日志、确定的重放顺序与保留期限；现有总线只提供最新快照，不能从版本号恢复遗漏帧。缺帧或坏快照时按明示的冷启动/全量重算与信号降级规则处理。
3. **业务一致性验收门禁**：
    - 在输入日志连续且状态快照有效时，强退重放后的信号逐帧等于不中断参考运行；缺帧或坏快照时验证降级与重新建立状态的边界，不承诺无法重放的历史帧仍完全一致；
   - 在真实 Windows 环境下实测进程启动与恢复时延。

---

### 实验性探索项（独立分支验证，不列入主线前提）

1. **共享内存（mmap / IPC Shared Memory）**：
   - 须攻克 Windows 文件锁、进程崩溃孤儿句柄、写完再发布及读者防撕裂读机制；
2. **GC 阈值微调**：
   - 仅当 GC 停顿被测为瓶颈时，在长周期压测中对比默认阈值与调整阈值的实际停顿分布及内存增长；
3. **数值类型降型（Downcasting）**：
   - 逐列审计精度（价格需 2~4 位）、大金额（数十亿防止溢出）与空值表示，不做全表粗暴降型。

---

## 四、阶段准入与回退门禁

以下 200ms、100ms、30% 仅可作为阶段 0 采样时的观察标签，不能在缺少机器、数据量、盘中负载和用户可接受延迟定义时充当固定工程门槛。阶段 0 后应先固定测试环境、输入集、采样窗口、P95/P99 与正确性阈值，再决定性能改造是否准入。

| 阶段 | 准入决策 | 回退条件 |
| :--- | :--- | :--- |
| **1 排序** | 完成三种保留物理行策略的同源回放；仅在排序对交互延迟有可测影响且候选方案有净收益时采用 | 移动/分批路径变慢，或最终行序、焦点、选中、刷新中途操作不一致时，保留现有全量路径 |
| **2 总线** | 所有权审计必做；逐轨证明无发布后写入、无可写别名且内存/延迟收益可测，才移除对应拷贝 | 发现任何旧快照污染、版本错配或并发异常，恢复该轨隔离拷贝 |
| **3 差分** | 重建结果错误立即进入正确性修复；提速须证明绝对耗时和占比均构成瓶颈 | 新比较器不保真时退回已验证的旧比较器；若旧协议不能表达空值变化，仍须保留该情形的全量回退，不能仅退回 `compare()` |
| **4 虚拟化** | 阶段 1 后仍确认物理行数量是主要瓶颈，并有独立交互回放环境 | 焦点或快捷键错股立即关闭灰度；滚轮/布局问题按复现样本及延迟门槛回退 |
| **5 信号迁移** | CPU/GIL 关联及扣除 IPC、快照、重放后的净收益均经实测成立 | 有连续输入仍无法重建一致状态，或恢复时间超预算，则保留原执行路径；缺帧按预定义降级处理 |

---

## 五、优化方案效益指标与验证门禁表（闭环版）

| 核心指标 | 当前现状 (以阶段0实测为准) | 优化后目标 | 验证与验收门禁 |
| :--- | :--- | :--- | :--- |
| **采样指纹遗漏率** | 两处抽样存在假阴性风险，实际影响待测 | 对预期应更新的帧不发生持续遗漏 | 独立参考计算逐帧核对；报告暂时跳过、补齐及持续遗漏各自数量 |
| **主表排序耗时** | 阶段 0 测 P50/P95/P99 与行序变化率 | 选择全量、增量或移动方案中的实测最优者 | 最终行序、选中、焦点、快捷键和显示结果与基准一致 |
| **增量刷新阻塞** | 测 Tcl 调用次数和单回调最大耗时 | 在正确性不退化下减少主线程占用 | 含异步批次完成时间的端到端延迟改善 |
| **差分比对与正确性** | 测 `DataFrame.compare` 绝对耗时、占比和重建错误 | 先修复空值/结构语义，再按净收益决定是否按类型比对 | 两条轨各接收端逐帧等于基准，无法安全编码时全量回退 |
| **总线内存占用** | 按实际非空轨统计拷贝次数与内存峰值 | 仅移除已证明安全且有收益的拷贝 | 并发读写回放中快照与版本一致，无非法原位写入 |
| **跨进程推送** | 测静态/动态客户端、重连和首次同步 | 保留现有订阅与探测语义 | 所有接收链能处理对应协议版本及首包全量 |
| **信号计算开销** | 测计算、IPC、启动、快照及恢复成本 | 净收益为正时才迁移 | 输入连续时强退重放逐帧匹配；坏快照与缺帧有明确降级边界 |

---

## 六、方案修订确认

1. **正确性闭环**：补充采样指纹漏更排查、两轨差分接收端空值契约、所有权审计及信号快照与连续输入重放条件；
2. **实事求是**：修正了四个新增热点的事实描述，将高侵入性阶段重构为准入触发的候选改造；
3. **本次审计范围**：仅修订本规划文档，对当前工作区已有的实现代码与测试不作变更。

---

## 七、2026-09-24 最新实施审计（只修订方案，不变更代码）

本机复跑三份专项测试为 **9/9 通过**。新测试确证了 XOR 特例并验证有序哈希的选定样本；双轨测试只复写布尔门禁，排序测试仍使用 MockTreeview，三者均未直接驱动生产发送/接收与真实 Tk。下表替代上一轮审计状态。

| 优先级 | 已核实的实际状态 | 尚需完成的设计门禁 |
| :--- | :--- | :--- |
| **P1 哈希覆盖仍不完整** | `instock_MonitorTK.py:6398-6429` 已将列哈希改为带列名的有序组合，消除了“同值双列 XOR 归零”特例；但计算入口仍只列举有限字段，UI `:17127-17143` 仅加入 `current_cols[:10]`。列哈希异常被 `except: pass` 静默跳过，且有限位哈希不能证明“绝不误判”。 | 以真实入口回放第 11 个及之后的展示列、仅大周期轨变化、对象/扩展类型、哈希失败与相同时间戳更新；逐帧对照独立真值，不再使用“100% 覆盖全部展示列”。 |
| **P1 发送失败后的全量自愈未落地** | `instock_MonitorTK.py` 的 `_cold_start` 赋值仅在初始化、主动全量请求和冷启动重置处；Pipe 异常 `:9236-9240` 与 Socket 异常 `:9431-9434`、`:9459-9468` 均未设置它。`_last_vis_bus_version`、`_last_send_df_hash` 及前帧缓存仍在发送前推进。 | 明确显示轨及逐订阅端的成功/确认版本；失败时保留待发全量并证明下一次实际发送为全量。未通过前，`20260924_1410_task.md` 的“异常重连自动物理校准”不成立。 |
| **P1 日线门禁只完成第一层解耦** | `:9268-9273` 已改为两轨均空才跳过 Socket 分支，显示轨空时日线全量具备进入分发的条件；但活跃订阅端仍受 `:9367-9373` 发送间隔及 `:9275` 冷却限制。若全量回退帧被节流，前帧缓存已推进，后续差分可能基于订阅端未收到的版本。 | 回放“显示轨空、日线变空、端口仍在间隔内/冷却中、随后仅另一字段变化”；检查 26670/26671/26675 及动态端口的实际收包与缓存一致性。 |
| **P2 借读隔离是局部改进** | `:9005-9010` 在 TWAP 注入前复制两轨，降低 `send_df` 污染总线原对象的风险；当前对同源别名也可能执行两次复制，且总线所有权审计未完成。 | 验证总线快照发布前后内容、别名及旧版本生命周期；记录新增复制的 RSS/耗时，再决定是否开展阶段 2 去拷贝。 |
| **P2 阶段 0/1 仍未完成方案验收** | 阶段 0 测试用合成 DataFrame 与重复实现的哈希助手，尚无真实行情 Golden Samples 或五场景 P50/P95/P99、CPU、RSS；阶段 1 仍只用 MockTreeview，不代表 Tcl DOM、实际焦点与异步刷新。 | 将当前结果标为“缺陷复现与模拟策略评估”；按阶段 0/1 的真实输入、生产入口、真实 Tk 和端到端完成时间门禁补验。 |

**质量核对**：普通 `git diff --check` 只检查未暂存差异；当前改动已暂存，本机 `git diff --cached --check` 在 `20260924_1410_task.md:3` 报出行尾空格。该项不影响上述逻辑判断，但“格式零违规”仍需用正确范围复查。

**审计结论**：XOR 特例和原先的双轨总门禁已得到局部修正；阶段 0 完整基线、阶段 3 逐订阅端保真与失败自愈、阶段 1 真实 GUI 验收尚未完成。先关闭 P1 正确性门禁，再评价性能及后续候选改造。

---

## 八、最新提交复核与后续方案门禁（仅规划，覆盖第七节中过时的代码状态）

本轮确认：UI 指纹已遍历全部 `current_cols`；Pipe 和显示 Socket 失败会设置 `_cold_start`；日线全量包在端口选择层绕过逐端口 `dynamic_interval`；订阅端 Socket 失败会设置 `_force_sync_{port}`。三份专项测试本机复跑 **9/9 通过**，`git diff --cached --check` 与 `git diff --check` 均无报错。第七节“前 10 列截断”“异常未设置冷启动”及行尾空格的描述只代表上次审计时点，不再作为当前结论。

| 优先级 | 当前代码证据与残余风险 | 方案修订及验收条件 |
| :--- | :--- | :--- |
| **P1 异常列仍可静默漏更** | UI `instock_MonitorTK.py:17147-17157` 对无法哈希的列使用固定 `hash(col)+1`；同一列连续两帧都失败时，列值由 A 变 B，兜底值仍相同，指纹可能不变。计算入口 `:6397-6426` 的异常分支仍为 `pass`。 | 异常必须触发本帧保守刷新/计算或使用经过验证的内容编码，不可用列名常量代替列值；加入连续异常帧、对象/扩展 dtype 和可见第 11～50 列的生产入口回放。有限位哈希只能降低碰撞概率，禁止“必定触发”“零漏更”承诺。 |
| **P1 计算入口覆盖与时间语义待闭环** | `:6393-6429` 仅哈希列举字段和行数，未纳入代码/索引、展示轨 `full_df_res` 与其他计算依赖；同一 `snap_time` 下这些输入单独变化可被跳过。总线 `market_state_bus.py:76-77` 每次发布更新版本与时间戳，若时间戳每帧改变，当前哈希又无法有效防抖。 | 先列出计算输出的实际依赖，分别为原始轨、展示轨、配置与历史状态定义变更版本；用同时间戳变更、仅展示轨变更、代码交换、重复发布等样本核对正确性与命中率。保留性能结论之前报告哈希耗时及真实跳过比例。 |
| **P1 全量包只穿透了端口级节流** | `instock_MonitorTK.py:8918-8922` 的非交易时段停止自动发送、`:8971-8975` 的全局 300 秒门禁和 `:9277` 的 IPC 冷却都发生在 `:9369` 端口全量放行之前；因此“日线全量立即必达”尚不成立。 | 把待发全量视为持久的高优先级状态，明确全局门禁、非交易时段、冷却和端口门禁各自的穿透规则；测试在每一层受限时的实际发包和超时上界。只有收到对应版本的接收确认后才能称“必达”，否则称“已尝试发送”。 |
| **P1 失败与重连仍缺可执行重试闭环** | `:8999`、`:9049`、`:9103-9106` 在物理发送前推进已读版本、哈希和前帧缓存；总线 `get_latest_dual` 对相同版本返回 `None`。`_cold_start` 与 `_force_sync_{port}` 虽已设置，但在没有新总线版本时不会重取同帧重发。临时动态端口 `:9454-9457` 在失败后也被移除。 | 区分“已计算版本”“已尝试版本”“逐端口已确认版本”；失败后保留最新全量快照及待发端口，并提供与新行情无关的重试唤醒，直至成功或明确超时/退订。临时端口仅在确认或过期后清理；逐端口回放失败、静默行情、重连及乱序确认。 |
| **P1 强制请求状态由两条路径共用** | UI 拉取路径 `:6284-6287` 会读取并清除 `_force_full_sync_pending`，发送线程 `:8958`、`:8990-8996`、`:9040-9047` 也依赖同一标志；若 UI 先消费，发送端可能失去穿透与同版重发条件。`ATS_RECEIVED` 处理 `:3332-3349` 未核对确认版本便清除端口强制标记。 | 将 UI 强刷与 IPC 全量请求拆成独立、可持久化的请求状态；确认必须绑定端口、会话和同步版本，仅清除已确认版本的待发状态。用 UI 与发送线程交错执行及延迟/旧确认回放验证。 |
| **P2 测试与实测证据不足** | `tests/test_tk_perf_stage0_telemetry_and_golden_samples.py:95-123` 的 UI 助手仍保留 `current_cols[:10]` 和异常 `pass`，与生产代码不一致；双轨测试只复制布尔判断。9/9 通过不验证当前生产门禁、真实 Socket/Pipe、Tcl 完成时间或独立行情真值。 | 更新测试设计为直接驱动生产函数或抽取同一纯逻辑入口，加入双轨客户端回放、故障注入、真实 Tk 异步批次完成及五场景 P50/P95/P99。阶段 0/1 暂标“局部缺陷修复与模拟通过”，不得标完整验收。 |

**调整后的实施顺序**：先以最小改动关闭上述 P1 正确性与可达性门禁，并补独立 Golden Samples；再完成阶段 0 的性能基线与阶段 1 的真实 GUI 评估；阶段 2 去拷贝和阶段 3～5 候选提速仍按原准入条件决定。每一项需展示实际接收端状态与发送端待发状态的一致性，不以 `sendall` 成功或单元测试通过替代端到端验收。
