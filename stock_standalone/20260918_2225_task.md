# 任务清单: 2026-09-18 22:25 (集中交易指挥室暗黑分割线、手动添加标的标记与优先显示、数据防丢失双重持久化与底层新股自动同步)

## 任务目标
1. 修复集中交易指挥室中间左右分割线白色突兀高光问题，升级为与系统一致的极窄暗黑高质感科技灰配色；
2. 代码列彻底净化（纯 6 位数字代码，绝无 📌 污染与省略号截断）；标记打在 name（名称列）上，配合亮金 `#ffd700` 高亮加粗；
3. 不强制置顶，普通指标列按真实数值自然排序；操盘手点击“名称”表头时，手工标的高权重优先置顶排列；
4. 彻底根治数据丢失隐患，实现手动标的独立持久化豁免过滤、主配置与 .bak 镜像双写容灾自愈；
5. 实现底层新股次新股全自动异步增量感知与静默同步入池，无需每日手动敲入新代码。

## 任务拆解与进度跟踪
- [x] **【Task 1: 集中交易指挥室分割线样式修复】(`ts/ui/ipo_command_room_dialog.py`)**：
    - [x] QSS 中显式补充 `QSplitter::handle` 规则，设为 `#1c1e2d` 暗黑底色，hover/pressed 分别为 `#00e5ff` / `#00b0ff`；
    - [x] 设置 `handleWidth(2)` 为 2px 极窄尺寸，并在 `restoreState` 后锁定 2px 极窄尺寸，彻底消除原生 Windows 白色粗竖线。
- [x] **【Task 2: 代码列纯净化、名称列专属标记与点击名称排序优先】(`ts/ui/ipo_subnew_detector_dialog.py`, `ts/ui/ipo_command_room_dialog.py`)**：
    - [x] 数据层：引入并持久化 `manual_codes`；
    - [x] 代码列：严格只放纯 6 位数字（如 `300058`），绝无 `📌` 字符，彻底杜绝 `📌 ...` 截断；手工标的代码配合金色加粗；
    - [x] 名称列：专属徽标呈现在名称上（如 `📌 蓝色光标`），配合亮金 `#ffd700` 高亮加粗与专用 ToolTip 说明；
    - [x] 排序逻辑：涨跌幅、现价等普通列完全自然排序；点击“名称”表头时，手工标的高权重优先置顶排列；
    - [x] 重置保护：重置新股池时 100% 自动优先保留手工代码。
- [x] **【Task 3: 数据安全持久化与防丢失、防覆盖自愈机制】(`ts/ui/ipo_subnew_detector_dialog.py`)**：
    - [x] 建立 .bak 镜像备份与主损坏自愈恢复逻辑，主配置丢失损坏时 0 秒自愈恢复；
    - [x] 赋予手工代码最高意志免检权，防止 is_stock_actually_listed 误杀，并修复缺失上市日期时的过度过滤；
    - [x] Windows 文件锁写入重试机制 (3次退避重试)。
- [x] **【Task 4: 底层新股次新股全自动增量同步入池】(`ts/ui/ipo_subnew_detector_dialog.py`)**：
    - [x] 启动 2 秒后异步线程触发底层 NewStockFetcher 增量同步，之后每 15 分钟静默增量感知；
    - [x] 智能提取全市场已上市新股，自动合并追加至 monitored_codes，且保持手工标的处于最顶部。
- [x] **【Task 5: 自动化测试验证与全量回归】(	ests/test_ipo_persistence_and_auto_sync.py)**：
    - [x] 编写专项测试 6/6 绿灯通过；
    - [x] 关联测试集 (	est_ipo_command_room_persistence.py, 	est_ipo_fleet_trading_arbitration.py, 	est_ipo_detector_column_widths_persistence.py, 	est_ipo_vwap_sentiment_and_horse_race.py) 全部 100% 绿灯 (共 31/31 PASSED)。
- [x] **【Task 6: 任务状态与总结回写】(gemini.md, walkthrough.md)**：
    - [x] 更新 gemini.md 与总结报告。
