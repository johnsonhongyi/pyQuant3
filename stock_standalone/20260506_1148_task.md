# 20260506_1148 任务清单 - 竞价赛马面板存盘一小时防抖优化

## 核心任务目标
将竞价赛马面板的 UI 状态持久化存盘防抖延时（实盘模式）从原来的 10 分钟（600,000ms）调整为 1 小时（3,600,000ms），以进一步降低高频行情下的磁盘写入频率，优化系统在持续监控过程中的 I/O 吞吐开销。

## 实施详情
- [x] **防抖延时扩容**：在 `bidding_racing_panel.py` 的 `_trigger_save_ui` 触发函数中，将实盘模式的 delay 间隔值从 `600000`（10 分钟）物理修改为 `3600000`（1 小时）。
- [x] **注释描述同步**：将对应处的代码注释更新为 `# 回测模式下使用 1 秒防抖，实盘模式使用 1 小时防抖`。

## 完成状态
- [x] 完成 `bidding_racing_panel.py` 实盘保存延时扩容至一小时防抖
- [x] 完成任务清单与 `gemini.md` 的同步更新
