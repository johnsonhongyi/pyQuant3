# 2026-05-07 22:25 退出异常与 SyncManager 关闭故障根治任务清单

## 🎯 任务目标
- [x] **根治应用关闭时的 Windows 致命异常 (STATUS_ILLEGAL_INSTRUCTION 0xc000001d)**：在 `instock_MonitorTK.py` 的 `on_close` 方法中，当调用 `self._sync_manager.shutdown()` 时，由于其他后台常驻或 daemon 线程（如 `_bus_worker_thread`、`watchdog_loop` 等）可能在并发访问被关闭/断开的 `SyncManager` 共享代理字典而产生 native 级别的连接异常崩溃。
- [x] **安全断开并加入 `_bus_worker_thread` 监听线程**：在主程序关闭序列中，将 `_bus_worker_thread` 加入 STEP 2.5 显式 join 优雅回收。
- [x] **实现 GlobalValues 退出瞬间降级机制**：在 `_sync_manager.shutdown()` 之前，强制将 `GlobalValues` 标记为失效 (`_manager_dead = True`) 并清空 `_global_dict`，使得后续任何线程对 `getkey`、`setkey` 等的调用无缝降级走本地安全字典，彻底拦截了对 shutdown 期 socket/pipe 通信管道的访问。

## 🛠️ 实施内容
- [x] **修改 `instock_MonitorTK.py` 中的 `on_close` 逻辑**：
  - 在 `STEP 2.5` 增加对 `_bus_worker_thread` 线程的 `join(timeout=0.3)`。
  - 在 `STEP 4.3` 的 `_sync_manager.shutdown()` 之前，增加 `cct.GlobalValues._manager_dead = True` 和 `cct.GlobalValues._global_dict = {}` 强制失效降级块。
- [x] **更新 `gemini.md` 文件**：记录此次工程级稳定性更新。
