# Nuitka 编译配置对齐与计时功能同步任务清单

> 创建时间：2026-05-22 11:36  
> 任务状态：已完成 (Completed)  

## 任务目标
1. **同步编译计时功能**：将 `nuitka_build_console_onlyClang.bat` 里的秒级时间统计与持久化追加落盘（写入 `time.txt`）能力完美引入 `nuitka_build_console.bat` 中。
2. **对齐优化参数与依赖过滤**：
   - 物理拦截 12 款 PyQt6 重型垃圾 DLL 依赖，对齐 `--noinclude-dlls` 配置。
   - 排除 `datacsv` 全量大文件夹拷贝，改为只精确选择性打包 `search_history.json` 和 `minute_kline_viewer_history.json` 配置文件。
   - 补齐 `JSONData` 与 `JohnsonUtil` 两个主包及其子模块与非 Python 静态资产的精确打包映射。
   - 对齐隐藏诊断模块依赖 `--include-module=tk_gil_monitor` 以及 `--include-package=a_trade_calendar`。
   - 在 `onlyClang` 與普通编译脚本中均同步对齐 `--enable-plugin=pyqt6` 与 `--enable-plugin=tk-inter`，确保运行时 PyQt6 模块 100% 被正确编译打包。

## 实施详情

### 1. 计时逻辑同步 (Timer Hooks Setup)
- **启动锚点**：在批处理入口增加 Unix 时间戳提取与格式化输出。
- **计算与落盘**：编译完成时通过 Python 高精度计算总耗时，以 `HH:MM:SS (seconds)` 输出并追加写入至 `time.txt`。

### 2. 编译参数极致精简与对齐 (Configuration Parameters Alignment)
- **DLL 精准拦截**：
  `Qt6WebEngineWidgets`, `Qt6WebEngineCore`, `Qt6Pdf`, `Qt6Quick`, `Qt6Qml`, `Qt6VirtualKeyboard`, `Qt6Multimedia`, `Qt6Bluetooth`, `Qt6Svg`, `Qt6Sql`, `Qt6Test`, `Qt6Xml`
- **模块与包对齐**：
  全面使用包包含形式以解决 `a_trade_calendar`、`JSONData` 和 `JohnsonUtil` 动态懒加载时的分析遮蔽错误。
