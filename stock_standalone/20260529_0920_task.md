# 2026-05-29 09:20 Task - Implemented Auto-Scrolling to Bottom for Backtest Reports

## 任务目标
- 在打开个股回测报告或切换个股时，对话框文本区域自动置底滚动，优先展示最新的回测交易动作。
- 双端对齐：在 PyQt6 可视化端和 Tkinter 大屏选股端实现相同的自动置底视觉效果，且在显示“综合简报”时保留顶部视图不予置底。

## 实施详情
- 修改 `trade_visualizer_qt6.py` 的 `ScrollableMsgBox`：
  - 判断是否是回测标题。若属于回测报告，则在 `update_content` 中使用 100ms `singleShot` 延迟，安全将滚动条拉至最下方。
- 修改 `stock_selection_window.py` 中的 `BacktestReportDialog`：
  - 在 `.after(100)` 回调中调用 `.text_area.yview_moveto(1.0)` 置底。
- 修改 `instock_MonitorTK.py` 的 `_show_strategy_report_window`：
  - 同步引入 `.after(100)` 文本框自动滚动置底。

## 验证结果
- [x] 双端回测窗口打开时全部平滑置底，完美呈现最新的最新交易决策，且不打扰综合简报顶部排版。
