# 2026-05-29 09:00 Task - Implemented Signal Toggle & Live Log Query Bypassing

## 任务目标
- 在 K 线图控制条新增 "信号" 复选框开关，默认不显示信号，以避免高频 Tick 监控时的视觉噪点。
- 当开关关闭时，彻底跳过后台日志 CSV 文件的读取与解析，从源头上进行物理级 IO 资源节流。
- 开关打开时，按需即时重载实盘日志并重绘图表，且开关状态在 `visualizer_layout.json` 中自动持久化。

## 实施详情
- 修改 `trade_visualizer_qt6.py`：
  - 工具栏加入 `self.signal_show_cb = QCheckBox("信号")`，默认设为 False。
  - 重构 `DataLoaderThread`：在数据流拉取判定中，若开关关闭，则跳过 `logger.get_signal_history_df()`，极大降低 CPU 开销。
  - 在 layout 存储中保存与加载 `show_signals` 键值。

## 验证结果
- [x] 开关切换流畅，关闭时实盘日志读取完全被跳过，极速重绘。
- [x] 成功写入 visualizer_layout.json 跨会话加载。
