# 2026-09-19 03:15 任务记录：新股检测中心与集中交易中心接入系统语音告警（全仓轮动换股、新股申购、开盘首日高级信号与全生命周期信号复盘迭代日志）

## 任务背景与操盘手明确指示
- 用户现场指示 (P0)：
  1. “检测中心,集中交易调度室资金的轮转交易需要全仓轮动的换股切换持续每日操作的能力,全面接入ats的报警api中,还需要更高级的信号方式”；
  2. “新股次新股的申购,开盘首日的高级信号通知能力”；
  3. “交易中心有信号日志,点击详情查看,迭代日志,不是今天卖了就没下文了”；

## 核心设计与全体系落地成果 (100% 完成)
1. **全仓轮动换股机制与持续每日操作能力 (`ats/strategy/ipo_trading_center.py`)**：
   - 集中交易调度室新增 `ROTATION_FULL_CAPITAL`（全仓轮动）交易模式与 `enable_full_rotation` 响应；
   - 当持有标的走弱/破位且全池赛马涌现 Rank 1 领头羊时，生成原子 `FULL_ROTATION_SWAP` 指令：
     - 第一阶段：100% 平仓退潮老标的，资金回笼并核算收益沉淀战绩；
     - 第二阶段：100% 满仓集中买入新龙头接力，实现每日滚动的全仓腾挪接力。
2. **彻底解决“今天卖了就没下文”：交易账本持久化与全生命周期复盘迭代日志 (`ats/strategy/ipo_trading_center.py`)**：
   - 持久化账本：在 `ipo_trading_ledger.json` 中完整记录 `active_positions`（活跃持仓）、`closed_positions`（已平仓历史战绩，记录买卖价、平仓收益率、离场原因与复盘说明）与 `signal_iteration_log`（全生命周期信号决策日志）；
   - 支持跨实例与冷启动原子落盘，实现有始有终的量化交易闭环。
3. **集中交易指挥室双模式切换与详情复盘查看 (`ats/ui/ipo_command_room_dialog.py`, `ats/ui/ipo_arbitration_detail_dialog.py`)**：
   - 持仓卡片支持 `[🟢 活跃持仓 (N)]` 与 `[📜 历史平仓战绩 (M)]` 自由切换；
   - 指令卡片支持 `[⏳ 待执行指令 (N)]` 与 `[📋 历史信号日志 (M)]` 自由切换；
   - 双击或右键点击表格任一行即弹出 `IPOArbitrationDetailDialog` 集中仲裁详情窗，高亮展示历史平仓胜负手、入场理由、止盈止损线、换马接力标的与迭代日志；
   - 顶栏增加 `[⚡ 全仓轮动模式 (100%接力新龙头)]` 勾选框与 `[🔊 语音播报: 开/关]` 控制。
4. **今日新股次新股申购嗅探与即时广播 (`ats/strategy/ipo_trading_center.py`, `ats/ui/ipo_subnew_detector_dialog.py`)**：
   - 集成权威日历接口，自动嗅探今日处于可申购状态（`apply_date == today`）的优质新股；
   - 启动与早盘自动触发语音 TTS 广播（“今日新股申购提醒，代码...，发行价...，请及时在交易软件中申购”）与多屏自适应 Toast 弹窗，单日去重。
5. **开盘首日四大高级形态战术分级 (`ats/strategy/ipo_vwap_detector_engine.py`)**：
   - 战术分级体系：👑 `SSS 绝杀级`、🥇 `S 级接力`、🎯 `A 级潜伏`、🚨 `ALERT 警报`；
   - 首日四大高级形态：
     - 👑 `首日早鸟贴线吸筹 (SSS级)`：全日紧贴 VWAP 惜售，锁定极低成本；
     - 👑 `首日低开恐吓放量反包 (SSS级)`：早盘低开深蹲诱空洗盘后拔地放量反包均线；
     - 🚨 `首日临停冲高算法挂单高抛 (ALERT级)`：多次临停加速狂热，计算机提前算法挂单锁定顶点止盈；
     - 🚨 `首日阴跌破位避险 (ALERT级)`：首日下破分时均线且无筑底结构，一票否决坚决避险。
6. **升级 ATS 统一报警中心与直达定位 (`ats/alert_notifier.py`)**：
   - 新增 `notify_trading_signal(...)` 交易信号专属通知通道；
   - 扩充全套专业交易播报词典；
   - 识别窗口纳入检测中心与指挥室，实现 `locate_stock_in_table(code)` 秒级唤醒置顶并高亮聚焦标的行。
7. **SBC 出厂默认尺寸升级与实例环境隔离 (`ats/ui/intraday_strategy_dialog.py`)**：
   - 将 SBC 走势图默认基准尺寸由 680x420 全面升级为图 2 舒展舒适尺寸 `800x560`；
   - 完善 `IPOTradingCenter` 内存实例与磁盘账本隔离机制，单元测试纯内存运行不污染生产账本。

## 自动化测试全量验证 (38/38 PASSED 100% 绿灯)
- `tests/test_ipo_detector_and_trading_center_voice_alert.py` (7/7 PASSED)
- `tests/test_ipo_persistence_and_auto_sync.py` (11/11 PASSED)
- `tests/test_ipo_fleet_trading_arbitration.py` (8/8 PASSED)
- `tests/test_ipo_vwap_bottom_base_preorder.py` (6/6 PASSED)
- `tests/test_ipo_vwap_sentiment_and_horse_race.py` (6/6 PASSED)
