# 20260506_1150 任务清单 - 一键直达指数添加 F3-F8 快捷键对齐

## 核心任务目标
在 K线可视化主面板的菜单栏中，为“一键直达”的各大指数按钮配置键盘快捷键。快捷键从 `F3` 开始依次向下递增对齐（F3 至 F8），提升盘中通过键盘极速切换主流指数大盘行情的操作流畅度。

## 实施详情
- [x] **循环追加快捷键绑定**：在 `trade_visualizer_qt6.py` 的指数快捷 Action 构建循环中，引入 `enumerate` 索引 `i`，动态生成快捷键 `shortcut_str = f"F{3 + i}"`（从 F3 开始分配到 F8）。
- [x] **绑定快捷键与上下文**：
  - 调用 `act.setShortcut(QKeySequence(shortcut_str))` 绑定功能键。
  - 设置 `act.setShortcutContext(Qt.ShortcutContext.ApplicationShortcut)` 以确保在全局应用范围内快捷键的高效响应。
- [x] **优化 UI 文本与悬浮提示**：
  - 按钮显示文本从单纯的 `name` 优化为 `f"{name}({shortcut_str})"`（例如：` 上证(F3)`），让快捷键在菜单栏顶层直观可见。
  - ToolTip 更新为 `f"一键直达 {name.strip()} ({code}) [{shortcut_str}]"`。

## 指数快捷键对齐列表
1. **上证指数 (999999)** ─── **`F3`**
2. **深证成指 (399001)** ─── **`F4`**
3. **创业板指 (399006)** ─── **`F5`**
4. **北证50 (899050)** ─── **`F6`**
5. **创业板ETF (159915)** ─── **`F7`**
6. **科创50 (999688)** ─── **`F8`**

## 完成状态
- [x] 完成 `trade_visualizer_qt6.py` 中 `index_map` F3-F8 快捷键绑定与文字显示优化
- [x] 创建并归档本任务清单 `20260506_1150_task.md`
- [x] 在 `gemini.md` 中记录本次迭代日志
