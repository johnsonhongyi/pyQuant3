# 20260418_1909_task

## 目标
- 修复 `tdx`, `ths` 联动的状态失效问题（即使在界面中取消勾选仍会发生外部联动）。
- 更新 `link_manager` 使其能够自动跟进及判定 `instock_MonitorTK.py` 中的 `tdx_var`, `ths_var`, `dfcf_var` 当前状态。

## 完成情况
- [x] **重构 `linkage_service.py` 状态承载**：修改 `LinkageManagerProxy.push` 接口和 `LinkageService._execute` 执行端，允许传递及接收 `tdx`, `ths`, `dfcf` 的布尔值标志位。这使得多进程队列不仅能够承载投递意图，还能够承载界面一侧复选框的实时状态切片快照。
- [x] **修复 `stock_sender.py` 中 IPC 转发的侧漏**：在原有架构中，`StockSender.send()` 调用 `get_link_manager().push` 时抛弃了所有 Tk 界面变量信息。已修改增加 `**flags` 传参，将界面组件（`self.tdx_var` 等）的快照通过 IPC `push` 一并打包送入后台进程进行解耦发送。
- [x] **对齐 `instock_MonitorTK.py` 直调层代码逻辑**：将所有的 `self.link_manager.push(code, ...)` 直调层修改，强制注入 `tdx=bool(self.tdx_var.get())`、`ths=bool(self.ths_var.get())`、`dfcf=bool(self.dfcf_var.get())` 参数，确保鼠标选取、键盘上下翻页、及直接点击都能被准确追踪状态。
- [x] 成功执行了全量 Python 严格语法校验及编译预取，代码正确无误地生效。
