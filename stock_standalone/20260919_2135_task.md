# 2026-09-19 21:35 任务计划：长期通道底部结构次级买点最小可跑闭环 (P0实施)

## 任务背景与核心目标
- **操盘手现场明确指示**：
  1. L0 1s~3s，L1 15s~30s，L2 秒级(仅5~10只强候选)；
  2. 统一 signal_level (S0~S5) 与 quality_grade (A/S/SS)；
  3. 首日新股与次级买点 Tag 分开 (IPO_BID_SURGE, IPO_VWAP_STABLE, SUBNEW_PULLBACK_REENTRY, CHANNEL_SECONDARY_BUY)；
  4. 指令收敛为 BUY_SCOUT, BUY_CONFIRM, EXIT_ALL，FULL_ROTATION_SWAP 仅作展示不自动执行；
  5. 按 P0 最小切片落地：先把 强信号 -> TradePlan -> 退出保护跑通，UI/SBC图元放 P2。

---

## 任务执行清单 (P0 最小闭环)

- [x] **Step 1: 新增独立策略模块 ts/strategy/channel_secondary_buy_strategy.py (P0)**：
  - [x] 定义 SecondaryBuyStage 6 状态机 (DESCENDING_CHANNEL, BASE_DRYUP, FIRST_BREAKOUT, PULLBACK_STABLE, SECONDARY_BUY, INVALIDATED)；
  - [x] 定义 signal_level (S0~S5) 与 quality_grade (A/S/SS)；
  - [x] 定义 IPOTradePlan 数据结构；
  - [x] 实现 evaluate_channel_secondary_buy(df_60m, df_day, current_quote) 纯函数；
  - [x] 实现状态转移追踪与次级买点确认逻辑。

- [x] **Step 2: 在 ts/strategy/ipo_trading_center.py 中接入 TradePlan 与指令收敛 (P0)**：
  - [x] 接入 IPOTradePlan 导入与生成方法 create_trade_plan_from_signal()；
  - [x] 支持 4 大正交 Tag (IPO_BID_SURGE, IPO_VWAP_STABLE, SUBNEW_PULLBACK_REENTRY, CHANNEL_SECONDARY_BUY)；
  - [x] 指令动作收敛为 BUY_SCOUT, BUY_CONFIRM, EXIT_ALL；FULL_ROTATION_SWAP 降级为建议不自动执行。

- [x] **Step 3: 升级 ts/proactive_exit_engine.py 动态挂接 TradePlan 结构防守 (P0)**：
  - [x] 支持读取 extra_ctx[higher_low_stop] 与 extra_ctx[trade_plan]；
  - [x] 跌破次低点 Higher-Low 触发精准结构止损；
  - [x] 触及通道中轴第一目标位自动触发保本推移锁定胜果。

- [x] **Step 4: 加固 ts/tdx_realtime_fetcher.py 多周期 K 线安全 TTL 缓存 (P0)**：
  - [x] 为 60F（30s TTL）与日K（180s TTL）增加内存级缓存防护，杜绝网络高频重拉。

- [x] **Step 5: 编写自动化测试全覆盖并验证闭环 (P0)**：
  - [x] 新建 	ests/test_channel_secondary_buy_strategy.py (8/8 PASSED)；
  - [x] 覆盖：通道企稳、首阳试盘不买、缩量回踩抬高、次级买点放量突破、破位失效；
  - [x] 覆盖：TradePlan 数据生成与风控网络；
  - [x] 覆盖：ProactiveExitEngine 结构止损联动与保本推移；
  - [x] 运行全量核心回归测试 39/39 项 100% 绿灯全部通过。
