# 2026-07-06 11:00 任务清单：修复 `异动联动.py` 窗口配置文件 `window_config.json` 编码加载 Bug

## 当前任务
- [x] **修复 `window_config.json` 编码加载 Bug (Fix window_config.json UnicodeDecodeError)**
    - [x] **物理修复 `异动联动.py` 中的 `load_window_positions`**：在读取 `CONFIG_FILE`（即 `window_config.json`）时，统一指定 `encoding="utf-8"`。为了确保绝对兼容性，对已存在的可能使用 GBK 保存的旧配置文件进行多重编码解析尝试（首先尝试 `utf-8`，若失败则退避尝试 `gbk`），并在两者皆失败或损坏时安全捕获异常，回退至默认配置而绝不发生崩溃退出。
    - [x] **物理修复 `异动联动.py` 中的 `save_window_positions`**：在写入 `CONFIG_FILE` 时，统一指定 `encoding="utf-8"` 并设置 `ensure_ascii=False` 以支持直观写入中文字符，保障编码的持久化一致性与 UTF-8 规范。
- [x] **更新 `gemini.md` 变更日志**
